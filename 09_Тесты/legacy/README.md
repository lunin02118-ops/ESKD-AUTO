# Прежний набор тестов Tier 1–4 (надстройка v5)

Перенесён из `08_Результаты_Тестирования/Test_Suite` по плану рефакторинга (WP-0.2). Хранится для сравнения
и в прогон `09_Тесты/run_tests.py` не входит.

- `test_tier1_static_sanity.py`, `test_tier4_provisioning.ps1` — статические и установочные проверки эпохи v5;
  актуальные проверки — T0 в `09_Тесты/tests/test_static.py`.
- `test_tier3_sw_integration.py` — сценарии в живой сессии SolidWorks, ожидающие поведения v5
  (алиасы подписей, центрирование массы). Против v6 не запускать: проверяет то, что v6 намеренно не делает.
- `run_all_tests.py`, `TEST_REPORT.md` — прежний раннер и последний отчёт.
- `test_tier2_engine_units.py` удалён: это был Python-эмулятор ядра v5; его случаи перенесены в юнит-тесты C#
  `09_Тесты/unit/CoreTests.cs`.
