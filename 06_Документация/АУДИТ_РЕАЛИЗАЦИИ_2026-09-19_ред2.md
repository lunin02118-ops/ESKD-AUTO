# Аудит реализации ЕСКД-AUTO — 19.09.2026, редакция 2 (вечер)

Состояние: коммит `41c7f71` (ветка `feat/kto-folder-structure`), `main` на GitHub — `7794793` (PR #9), выпуск на NAS
`2026.09.19.2021` (коммит `173b791`). Аудит только на чтение: код, git, документы, отчёты тестов, NAS. Сверены все планы:
глубокий аудит 19.09 (разделы 3–11, волны 1–4), ТЗ-01, ТЗ-02 с планом и ТЗ-02б/в, ТЗ-03/03а, ТЗ-04 с планом, план
развёртывания 17.09, план устранения замечаний 19.09, журнал замечаний владельца, реестр дефектов. Каждый пункт проверен по
коду, а не по сообщениям коммитов. Предыдущая редакция — `_Архив/АУДИТ_РЕАЛИЗАЦИИ_2026-09-19.md` (утро, коммит `cbbcdc8`).

## 1. Коротко

* **Код соответствует планам.** Все кнопки ТЗ-02 работают и покрыты e2e; успешный путь «Готово к производству» до PDF
  через Excel (G05) и IGS по оси трубы (X05) проверены по сути. ТЗ-04 и план устранения замечаний выполнены. Критичные
  находки глубокого аудита по ядру, книге ЛЗК, макросам SWPlus и скиллу папок закрыты; Я-К4 — частично.
* **Выпуск на NAS совпадает с репозиторием:** все 105 хешей `toolkit_release.json` сходятся, выборочная сверка с git — тоже.
* **Реестр дефектов пуст:** 83 записи, 82 исправлено, Д-45 «не делаем» (решение владельца).
* **Приёмки нет:** ни одно замечание З-1…З-10 не отмечено владельцем «принято», чек-лист WP-8 и протокол П-2 пусты.
* **Тесты:** static 52/52; последний полный e2e — 134 прошли, 1 пропуск (Y05 — живая проверка NAS), 2 ошибки M07.
  M07 падает во **втором** длинном прогоне подряд и проходит отдельно — это не разовый сбой, а нестабильность длинной
  сессии. Полный e2e шёл на `2db5dc3`; после пересборки макросов (`173b791`) прогонялись static и группы S, M.
* **Волна 4 глубокого аудита (тесты и документация) не начата;** из 7 решений владельца принято одно (волна 1).

## 2. Что задумывали и что получилось

### 2.1. Глубокий аудит 19.09 — разделы 3–5 (ядро, кнопки и ЛЗК, развёртывание)

| Группа | Исправлено | Частично | Не исправлено | Ждёт владельца |
|---|---|---|---|---|
| Ядро (Я-) | К1, К2, К3, В1, В2, В6; В3 в основном | К4 (ожидание 8 с вместо ≤ 5, `Thread.Sleep`), В5 (сам эталон хешируется целиком) | В4 масса на каждом сохранении, В7 «Сохранить как» без нового имени (`EventHub.cs:345`), В8 подписки при ошибке `Track` (`EventHub.cs:495`), В9 окно настроек дублирует `Settings`, В10 интеропы и предупреждения сборки; желательные | — |
| ЛЗК и кнопки (Л-) | К1…К6, В1…В5, В7; торцовка — с одного конца | В8 (версия и масштаб DXF не задаются), В12 (б — да; а, в — нет) | В10 вложенные сгибы, В11 кг/м = масса ÷ длина, В14 «Габарит» вне словаря | В6 хеш по составу, В9 многотельный прокат (пока метка «оценка»), В13 версия Excel |
| Развёртывание (У-) | К1, К4, В1, В2, В9 | К2 (автозапуск окна через 5 с остался), В6 (`Use Software OGL=0` для любых карт) | В3 `Stop`/`Invoke-EskdStep`, В4 длинные пути, В5 запуск «от администратора», В7 версия SW по HKCU, В8 Uninstall без HKLM | К3 сброс профиля |

### 2.2. Глубокий аудит 19.09 — разделы 6–9

| Раздел | Итог |
|---|---|
| 6. Тесты | Не исправлены Т-К1 (R01 в одной сессии; падение SW в R01 устранено `27c64b1`), Т-К2 (8 ключей настроек), Т-К3 (правила golden master, AD-18 → несуществующий D-3), Т-В2…В6, В8, В9, скорость. Т-В7 — частично (порядок правок записан; пустые строки как отличие в `swplus_wp43_edits.py:174–185`) |
| 7. Макросы SWPlus | Закрыт: М-К1, М-К2, М-В2…М-В7 — код, T0, e2e S08–S11. М-В1 — только в Markdown (PDF памяток от 15.09) |
| 8. Скилл kto-folders | Закрыты П-К1, П-К2 и почти все важные. Открыты: три правила «Списания» (см. 3.2), тест длинного пути, правка ТЗ-03 |
| 9. Документация | Д-К1 (единый источник путей) и Д-К2 (статусы) не исправлены; Д-К3 (PDF отстают) ждёт решения № 7; ссылки: `README.md:74` → нет папки `07_Драйверы_NVIDIA` |

### 2.3. ТЗ-01 — установщик и план развёртывания

Упрощённый вариант ТЗ-01, принятый владельцем, готов: Install/Check/Uninstall, коды 0/10/20/40 в Check, SHA-256 выпуска,
журнал установки, отказ при несохранённых документах, многопользовательский режим (Т-56/57). Не делались (принято):
Repair/Export, протокол JSON, атомарное обновление, матрица размещения макросов, `toolkit_site.json`, журнал выпусков,
фаза машины. Т-38 (очистка `TT.TXT`) и тесты N01–N06, N12–N17 — нет.

План развёртывания: этапы 0.2 (токен убран из `origin`), 2 (публикация), §10 — сделаны. Ждут сисадмина: этап 1 (группы,
снимки, диски), 2.5 (права «только чтение» на папку выпуска — сейчас у большинства учётных записей есть запись), права на
`_Архив`, чтение PDF цехом (Р4-7). Ждут владельца: 0.1 (отозвать старый токен), Р-6 (репозиторий на GitHub открытый).
Этот ПК стоит на выпуске `2026.09.17.1936` — не обновлён; этап 4 (проверка в SolidWorks) не проводился.

### 2.4. ТЗ-02 — кнопки надстройки

| Пункт | Состояние |
|---|---|
| К-1…К-8, Т-9, Т-13, Т-15/17, Т-56…Т-58 | сделано, e2e |
| Т-1 состав вкладки | отступление: «Снимок эталона» только в меню, порядок не как в Т-1 |
| Т-2, Т-59 роли | не делается (решение владельца) |
| Т-3 занятые файлы `~$` с именем | частично: единой проверки нет |
| Т-7 путь ≤ 240 | только в выгрузке; «Сделать независимым», ревизия, снимок не проверяют |
| Т-16, Т-50 заметки ревизии `Revision2…5` | не доказано: при отсутствии заметки в форматке запись молча пропускается, V01 заметки не проверяет |
| Т-28 DXF | слой линий сгиба (Р0-7) не проверен ни разу — в X05 деталь без гибов, в X01 листовых нет |
| Ш11 | приёмки П-2 и метки `v6.3-tz02` нет |

### 2.5. ТЗ-03/03а и ТЗ-04

* **Структура заказа** в надстройке, скилле, шаблоне на NAS, инструкции, руководстве и «Как работает» — одинаковая.
  Расходятся: тело ТЗ-03 (§4.1, §4.6 — `_Производство`; §4.3 — ведомость в корне изделия; `_Архив` не описан), ТЗ-03а К-9,
  Памятки («имя папки изделия свободное», примеры без шифра), примеры регламента v3.1, ТЗ-02 (`04_ПРОИЗВОДСТВО`, `Y:`).
* `--check` строже ТЗ-03 §4.2: бракует короткое имя заказа, длину не проверяет.
* **ТЗ-04:** Р4-1…Р4-8 и Т4-1…Т4-5 сделаны (Р4-7 ждёт сисадмина). Не сделаны: защита листа и проверка даты, приёмка на
  копии 85_Т с тиражом 41 (и сверка раскроя, С-7). G05 не проверяет, что PDF участков ровно четыре и по странице.
  ТЗ-04 §2 описывает шаблон `ЛЗК_изделия.xlsx`, хотя книгу строит код.

### 2.6. План устранения замечаний 19.09

Этапы 1, 3–10 сделаны; 2 — частично (статусы); 11 (push, слияние, NAS) сделан, но в плане без статуса; 12 отменён
владельцем; 13 — В-2, В-3 даны, приёмки нет; 14 ждёт сисадмина.

## 3. Ошибки и замечания

### 3.1. Нужно исправить (найдено этим аудитом)

| № | Что | Где | Цена |
|---|---|---|---|
| Н-1 | ~~Справочник нормативов: пояснение «Торцовка — на каждый торец хлыста»~~ **Исправлено 20.09:** D4 — «один конец хлыста; второй конец уходит в захват». противоречит решению О-2 (расчёт верный, текст читает технолог) | `02_Шаблоны_и_Форматки/Справочники/Нормативы_производства.xlsx`, D4; на NAS тот же | 5 мин |
| Н-2 | Коммит `41c7f71` (окно настройки из выпуска на NAS) не в `main`: публикация из `main` вернёт старый exe | GitHub | слияние |
| Н-3 | ~~M07 падает во втором длинном прогоне подряд~~ **Исправлено 19.09 (`a96c5d9`):** надстройка снимала подписки внутри DestroyNotify, и SolidWorks изредка падал при закрытии детали (отчёты CXPA 17:53 и 19:04: нарушение доступа, DestroyNotify → Release). Теперь документ только помечается разрушенным, как в зонде; T0 `test_T0_destroy_notify_does_not_unsubscribe`; полный e2e — 135 pass, 1 skip, 0 ошибок | `Sw/EventHub.cs` | сделано |
| Н-4 | Полный e2e и unit не прогонялись на итоговом коде (`173b791`/`41c7f71`) | — | 25 мин |
| Н-5 | После сбоя переименования книги ЛЗК окно пишет «прежняя книга осталась на месте», хотя она уже в архиве (книга не теряется — лежит `.new`) | `LzkService.cs:870–906` | 15 мин |
| Н-6 | `Xlsx.Replace` переходит на запасной путь только при `PlatformNotSupportedException`; отказ `File.Replace` на SMB — `IOException`: журнал изменений 8 с повторяет и сдаётся, `.part` остаётся | `Xlsx.cs:686` | 30 мин |
| Н-7 | Правило «е» проверяет наличие по подстроке: «Деталь1» находит «Деталь10.dxf» | `CheckService.cs:437` | 15 мин |
| Н-8 | «Оптимально N хл.»: список по штукам — сотни МБ при тираже 100 000 (скорость исправлена, память — нет) | `LzkBook.cs:985`, `CutBar.Pieces` | 1 ч |
| Н-9 | Хеши выпуска считаются по файлам на NAS после копирования; exe, движок, модуль, `.reg` не хешируются — Check подмену не заметит | `Publish-EskdToolkit.ps1:118` | 1 ч |
| Н-10 | Lock-файл `~$40х20х1,5.sldlfp` в выпуске на NAS: публикация исключает `~$*`, а `/MIR` исключённое не удаляет | NAS, `Сортамент ГОСТ\Прямоугольная труба…` | вручную |
| Н-11 | На NAS установщик SWTools 1.1.109-LOCAL-TEST — с прежней ошибкой лицензии (48 ч); 1.1.110 тоже LOCAL-TEST | `03_Макросы_и_Плагины\SWTools_Установщик` | подписанная сборка владельца |
| Н-12 | Скрипты правок SWPlus передают неиспользуемый `ARCHIVE` (`99_Архив/SWPlus_до_шага4`); журнал решений (строки 234, 252) описывает архив и `--apply` отдельных скриптов без пометки «заменено» | `swplus_step4_edits.py:16`, `Журнал_решений_и_спайков.md` | 15 мин |
| Н-13 | Три правила для «Списания» (ТЗ-03:171, `structure.md:58`, `sort_plan.py:239–241` — сообщение «корень заказа», файл в `_Не разобрано\Списание`); папка `Списание` в корне NAS нигде не описана | скилл, ТЗ-03 | решение владельца + 30 мин |

### 3.2. Устаревшие статусы и тексты

* Журнал замечаний: З-1, З-3 — «ждёт публикации» (опубликованы в `2026.09.19.2021`).
* План устранения: этап 11 без статуса. ТЗ-02: «С-7…С-10 не выполнены»; §2 «DXF кнопка К-6» (надо К-2). ТЗ-02в: «вкладка
  как в Т-1». «Как работает система» — «состояние на 18 сентября». План развёртывания :81 — коды выхода 0/1/2/3.
* `README.md:285` Pipeline 2020 = 0, установщик ставит 1. Описание X05 «40х40» при трубе 30х30.
* Прежний аудит ЛЗК 18.09 (строка 81) — формула `2·Торцовка` (документ исторический).

## 4. Решения, которые нужны от владельца

1. **Приёмка:** отметить «принято» по З-1…З-10 (журнал) и пройти чек-лист WP-8 — без этого работа формально не закрыта.
2. Сброс профиля SolidWorks при каждом обновлении (У-К3) — оставить или только при первой установке.
3. Версия Excel в цехе (Л-В13, ТЗ-04 §2).
4. Хеш сборки по составу (Л-В6); полный расчёт многотельных сварных деталей (Л-В9).
5. Слияние документации (Д-Ж) и `pip install markdown` для пересборки PDF (Д-К3) — PDF памяток отстают на 4 дня.
6. Репозиторий ESKD-AUTO на GitHub открытый (Р-6); отозвать старый токен (этап 0.1).
7. Правило «Списания» (Н-13).
8. Подписанный установщик SWTools 1.1.110 для NAS (Н-11).

## 5. Что делать дальше (по важности)

1. Влить `41c7f71` в `main` (Н-2); исправить пояснение в справочнике нормативов (Н-1).
2. Полный e2e и unit на итоговом коммите (Н-4); разобрать M07 — отдельная сессия или повтор для длинного прогона (Н-3, Т-К1).
3. Мелкие правки кода: Н-5, Н-6, Н-7, Н-8, Н-12 (≈ 2–3 ч, каждая с тестом).
4. Обновить статусы и тексты раздела 3.2; единый файл путей и версий (Д-К1) — волна 4.
5. Хеши выпуска по репозиторию и для exe/движка (Н-9); удалить lock-файл на NAS (Н-10).
6. Остаток глубокого аудита: Я-В4, Я-В7, Я-В8, Л-В10, Л-В11, Л-В12, У-В3…У-В8, тесты Т-К2, Т-К3, Т-В2…В6.
7. Проверки, которых нет: слой линий сгиба в DXF на детали с гибами, заметки ревизии в штампе (Т-50), защита листа ЛЗК.
8. После решений владельца — пункты раздела 4; сисадмину — права и диски (2.3).

## 6. Дополнение 20.09.2026 — аудит после «ЛЗК из любой папки»

Три проверки (код надстройки, документация ↔ код, чистота репозитория) на `c4e91f7`.

**Исправлено в этом же выпуске:**
- Н-1 — пояснение торцовки в справочнике нормативов; Я-В8 закрыт ещё в `a96c5d9` (снятие подписок при ошибке `Track`).
- Книга ЛЗК вне структуры заказа: прежняя книга больше не удаляется без копии — копия в
  `%LOCALAPPDATA%\ESKD\ЛЗК_прежние`; не встала новая книга — прежняя возвращается, «.new» не остаётся; нечитаемая прежняя
  книга не затирается (отказ с объяснением; e2e L08). Запасное место «Документы» — только при запрете доступа, а не при
  любой ошибке записи; «файл открыт» на папке только для чтения больше не обрывает кнопку.
- Завершение ЛЗК больше не подписывается на `OnIdleNotify` и не отписывается внутри него (класс ошибки M07): опрос
  идёт из постоянной подписки EventHub; сбой окна итога не запускает завершение второй раз.
- Тихий запуск (COM) во время интерактивного больше не переводит идущий запуск в тихий режим.
- Сборка вне заказа, лежащая в базе/библиотеке: соседние модели базы не правятся.
- Папка «Крепёжная рама» у сборки вне заказа больше не делает всё изделие покупным (имя папки покупного — слово целиком).
- Сообщения «Готово к производству» и подсказка кнопки ЛЗК больше не требуют «01_3D».
- Мёртвый код: `LzkMaterials.IsRolledMetal`, `SwPlusMarkup.BlankShape`, `XlsxBook.Hide`; сдвоенные описания методов.
- Документация: «Как работает» (ЛЗК вне заказа), README (конвейер графики, несуществующая папка), план развёртывания
  (Т-56/57 сделаны), ТЗ-02 простыми словами (торцовка решена), пометки «заменено» в плане SWPlus ред4 и журнале;
  три отработанных аудита и ред3 плана — в `_Архив`; реестр `06_Документация/README.md`; битые ссылки в `_Архив`.
- Публикация больше не копирует на NAS файл `.git` (ссылка worktree); `.gitignore` без устаревших правил, `.claude/`
  игнорируется; удалён неиспользуемый `09_Тесты/tools/check_drew_activation.ps1`.

**Найдено, не исправлено (следующие шаги):**
- Выгрузка подсборки заменяет отчёт `_Экспорт.txt` изделия целиком (`ExportService.cs:111`) — нужен надёжный признак
  главной сборки (по обозначению `.00.00.000` он есть не у всех изделий).
- Для сборки вне заказа «Проверить изделие» считает браком ссылки на детали в других папках (`CheckService.cs:150–177`),
  выгрузка и проверка пропускают детали вне папки изделия (`ExportService.cs:157`), проверка не отсеивает исключённые
  из спецификации компоненты (`CheckService.cs:142`) — «Готово к производству» для такой сборки не пройдёт.
- Сервисы по-разному определяют папку изделия (`LzkNaming.ProductFolder` и `ProductLocator.Locate`) для сборки,
  лежащей глубже `01_3D`.
- «Снимок эталона» без `01_3D` отвечает «файлов: 0» (`EtalonService.cs`); «Закрыть заказ» пропускает изделия без
  `01_3D` без предупреждения (`CloseOrderService.cs:230`); ревизия по маске `stem*` может унести в архив PDF соседней
  детали без обозначения (`RevisionService.cs:361`); `Checksum` ловит только `IOException`.
- По-прежнему открыты Н-5…Н-13 (кроме изменений выше).

**Решения владельца (не менялось без него):** удалить на GitHub влитые ветки; судьба невлитой
`feature/tz02-eskd-buttons` (10 файлов, которых нет в `main`) и локальной `fix/standard-parts-gost2106`;
хранить ли `Настройка_Рабочего_Места_SolidWorks.exe` в git (18 версий, 177 МБ истории); что публиковать на NAS из
`06_Документация` и `SolidWorks_MCP_Server`; лишнее на NAS (`Legacy_Builds`, lock-файл `~$…`, `__pycache__`,
тестовый установщик SWTools 1.1.109); файлы вне git в рабочей копии `D:\Work\_dev\ESKD-AUTO`.
