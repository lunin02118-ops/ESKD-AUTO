# Журнал замечаний владельца

Сюда попадает всё, что владелец заметил в работе на живых заказах: замечание записывается сразу,
а правится отдельно — чтобы находка не потерялась, пока идёт другая работа. Ничего по записи
не правится, пока владелец не скажет.

Статусы: **записано** → **разобрано** (найдена причина) → **исправлено** (правка и тест) → **принято** (проверил владелец).

| № | Дата | Что не так | Где искать | Статус |
|---:|---|---|---|---|
| З-1 | 18.09.2026 | В спецификации на листе сборки не заполнена графа «Формат» у строки детали. На чертеже самой детали формат есть (`NC3-7R.03.002 Фланец` — в штампе «Формат А4»), а в спецификации сборки `NC3-7R.03.000 Перемычка` колонка пустая. По ГОСТ 2.106 графа «Формат» заполняется для каждого документа состава. | Проверить кодовую базу: откуда спецификация берёт формат — из чертежа детали (`Sheet.GetSize`/`GetTemplateName`) или из свойства модели; и заполняет ли его надстройка ЕСКД или SWPlus/SpecEditor | исправлено, опубликовано (2026.09.19.2021); доработано по З-15: столбец связан со свойством «Формат» модели, а его в SWPlus вписывают руками в MProp. Надстройка при сохранении чертежа пишет формат листов в «Формат» модели (разные форматы — «*)» и перечень в «Примечании»); D13. У старых изделий пересохранить чертежи. Уточнение Д-06: сохранение чертежа теперь пишет в модель, но только «Формат»/перечень форматов — на утверждение владельцу |
| З-2 | 18.09.2026 | При развёртывании рабочего места тип отображения по умолчанию на чертеже должен быть «Невидимые линии отображаются» (Настройки пользователя → Чертежи → Тип отображения по умолчанию). Сейчас в реестровом профиле `Drawings\Display Mode = 3`, а в живой сессии владельца `swHiddenEdgeDisplayDefault = 1` — нужное значение. | `01_Настройки_SolidWorks/Реестровые_Профили/01_SW2025_Корпоративный_Стандарт_ЕСКД.reg`, раздел `[…\Drawings]` | исправлено: в профиле `Drawings\Display Mode = 1` (подтверждено резервной копией настроек владельца 18.09 10:38), тест установки проверяет |
| З-3 | 18.09.2026 | Если в детали или сборке очистить свойства, повторный запуск заполнения работает неверно: не вносятся корректно ни фамилии, ни обозначение и наименование, ни разбивка материала — реквизиты после очистки заново не восстанавливаются. | Синхронизация реквизитов надстройки ЕСКД (`Sw/SyncService.cs`, `Sw/PropertyWriter.cs`, учёт происхождения значений — `Provenance`/`BchRecord`) и макросы SWPlus, если чистка шла через них | исправлено, опубликовано в выпуске 2026.09.19.2021 (41c7f71): «Удалить все свойства» MProp стирает и «Сводку → Автор»; при пустом свойстве MProp берёт первую строку своих списков, а установщик дописывал свою фамилию и организацию в конец — «Разработал» пустел, «Контора» становилась чужой; при пустом списке организаций MProp обрывался ошибкой 380 и не заполнял ничего. Теперь своя фамилия и организация — первыми (установщик и «Настройки ЕСКД»), MProp не падает на пустом списке и при пустом «Авторе» берёт первую фамилию; материал и прочие реквизиты надстройка возвращает при сохранении. M20; на рабочих местах — переустановка или «Сохранить» в «Настройках ЕСКД» |
| З-4 | 18.09.2026 | MProp (SWPlus) при выборе базы материалов падает: «Run-time error -2147352571 (80020005): Несовпадение типов», строка `CboGroupSW.AddItem Mid$(sTemp(i1), varTemp + 21, …)` в `CboBaseSW_Change`. | Причина — наша: скрипт добавления полимеров (коммит ac93277) сохранил `Библиотека_Материалов_ГОСТ.sldmat` с переводами строк LF, а MProp делит файл по `vbCrLf` и видел одну строку на весь файл | исправлено: CRLF возвращены (b2a55ac), тест T0 проверяет переводы строк, выпуск 2026.09.18.1307 на NAS |
| З-5 | 18.09.2026 | Материал не применяется: в окне «Материал» выбран «Полиэтилен ПВД 15803-020…», «Применить» — материал детали остаётся «не указан». Проверка через API: не назначается ни один неметалл библиотеки — ЛДСП, МДФ, фанера, ДВП/ХДФ, кромка, HPL, полимеры (45 из 128). | Внешний вид материала (`pwshader2 path`) указывал на файлы, которых нет в SolidWorks 2025: `\wood\oak\matte oak.p2m`, `\plastic	exturedine textured.p2m`. Металлы (пути верные) назначались | исправлено: внешние виды взяты из стандартных материалов SolidWorks («Дуб», «ABS»); проверка API — 128 из 128; тест T0 сверяет пути с установленным SW |
| З-6 | 18.09.2026 | Окно «Ведомость ЛЗК — операции»: по одним шифрам трудно проверять расцеховку — нужны эскизы (миниатюры) деталей и сборок в строках. | `Sw/LzkOperationsForm.cs` — добавить колонку-картинку; миниатюру брать из превью, сохранённого в файле SolidWorks, без открытия модели | исправлено, опубликовано (выпуск 2026.09.18.1857): колонка «Эскиз» (миниатюра проводника Windows из превью файла, в фоне), справа крупный эскиз выделенной строки |
| З-7 | 18.09.2026 | Покраска сборкой: считать только внешнюю площадь. Проверка: детали внутри окрашиваемой сборки на лист «Покраска» не попадают (двойного счёта нет), но площадь сборки = сумма полных поверхностей всех её деталей: внутренняя поверхность труб и грани в местах сварки/касания входят. NC3-7R: Опора 0,160 = Лежак 0,099 + Штанга 0,061; Укосина 0,144; Перемычка 0,062; Царга 0,104 м². У профильной трубы внутренняя поверхность ≈ 45 % площади — завышение почти вдвое. | `Sw/LzkService.cs` `Area()` — `CreateMassProperty().SurfaceArea` документа сборки | исправлено, опубликовано (выпуск 2026.09.18.1857): только наружная поверхность (труба — доля наружного периметра по сортаменту), сборка — сумма её металлических деталей; NC3-7R: Опора 0,160→0,084, Укосина 0,144→0,085, Перемычка 0,062→0,037, Царга 0,104→0,062 м² |
| З-8 | 18.09.2026 | В ведомости ЛЗК обозначение без исполнения: «Укосина» двумя строками `NC3-7R.02.000`, вторая должна быть `NC3-7R.02.000-01`. | Две причины: (1) синхронизация брала номер исполнения из имени корневой конфигурации — «01» производная от «00» и получала базовое обозначение (`Sw/SyncService.cs`); (2) ведомость сливала конфигурации одного файла в одну строку и читала обозначение активной конфигурации (`Sw/LzkService.cs`) | исправлено, опубликовано (выпуск 2026.09.18.1857; M06b); «Укосину» пересохранить |
| З-9 | 18.09.2026 | Спецификация на листе сборки `NC3-7R.03.000 Перемычка` ломается у детали БЧ `NC3-7R.03.001 Распорка`: запись материала выведена дважды (в строке «Распорка» дробь «Труба ПО 30х15х1,5 ГОСТ 8644-68 / 08пс ГОСТ 13663-86» с «L = 369 мм», наложенным на знаменатель, и ниже — ещё раз та же дробь и «L = 369 мм» отдельными строками), вёрстка таблицы кривая. Со слов владельца — после выгрузки в производство. | Запись БЧ пишется в «Наименование» (`Core/BchRecord.cs`, `Sw/BchService.cs`, `Sw/SyncService.cs`); журнал надстройки: при проверке изделия (К-3, 16:55) и каждом сохранении 17:10–17:11 «Наименование: Распорка → Распорка⏎<STACK…>» переписывается заново; деталь в это же время переименована «Распорка L369» → «Распорка» и перенесена в `01_3D` | исправлено (e80a27b), опубликовано (выпуск 2026.09.19.0855); детали БЧ пересохранить, SpecEditor перезапустить: SpecEditor писал первую строку записи в связанную ячейку — в модель; «Наименование» БЧ — одно название, строки — «Запись_БЧ»; S09, проверено на копии «Перемычки» |
| З-10 | 19.09.2026 | Спецификация на листе сборочного чертежа встаёт не на место — её приходится двигать. Должна быть привязана к левому нижнему углу: низ спецификации — внизу листа (на A3 и больших), как было изначально. | Точка привязки BOM форматок (`02_Шаблоны_и_Форматки/Основные надписи`, шаблон `Чертеж.drwdot`) и вставка таблицы в SpecEditor (`FrmSpecEditor:1708`, `InsertBomTable4(True, …, BottomRight)`) | исправлено (cbbcdc8), опубликовано (выпуск 2026.09.19.0855): точка привязки BOM на листах шире 395 мм — (205; 5) мм, у чертежей на прежней форматке SpecEditor переставляет её сам; S10 |
| З-11 | 20.09.2026 | Материал назначается правой кнопкой в дереве сборки, и деталь отдельно никто не сохраняет. Скрипт при этом не срабатывает, свойства детали остаются пустыми, в книге ЛЗК вместо материала «?» (изделие «Печка для Сергея»: 19 замечаний «не заполнено „Материал"» из 24 строк). Чтобы выгрузить предварительную ведомость в закуп, приходится открывать каждую деталь и жать «Сохранить». Нужен пакетный запуск одним нажатием. | Синхронизация висит на `FileSaveNotify` **документа** (`Sw/EventHub.cs`): при сохранении сборки событие приходит от сборки, а не от каждой детали. Кнопка «Синхронизировать» работала с активным документом (`SwAddin.cs`) | исправлено: «Синхронизировать», нажатая в сборке, обходит изделие целиком (`Sw/BatchSyncService.cs`) — каждой детали реквизиты, материал по геометрии и сохранение; берутся только детали из папки изделия. Тесты T07, T08. Изменено З-27 (шаг 1, 56b3572, PR #18): обход изделия и `Sw/BatchSyncService.cs` удалены — реквизиты, материал и сохранение деталей изделия теперь в окне «Проверить изделие» (`Sw/ProductReviewService.cs`); T07, T08 зелёные на коде PR #18 |
| З-12 | 20.09.2026 | Материал не сверяется с тем, из чего деталь сделана. Назначили «Лист 6 мм», а базовая кромка построена из 8 мм — в ЛЗК уедет 6 мм, и привезут не тот лист (боевой случай: «Передняя стенка» — обозначение `560×605×8`, материал «Лист Б-ПН-НО-6,0»). И наоборот: сделал сварную деталь из плоскоовальной трубы 30×15×1,5 — материал должен подтянуться из библиотеки сам, чтобы конструктор не вводил его по десять раз. Если подходящих несколько (лист 6 мм: Ст3сп и 09Г2С) — окно выбора. | Надстройка не смотрела на геометрию вообще: `SyncMaterials` (`Sw/SyncService.cs`) только переносила назначенный материал в свойства, а `Типоразмер` библиотеки не читался (`Core/MaterialCatalog.cs`) | исправлено: подбор по геометрии (`Core/StockCatalog.cs`, `Sw/StockService.cs`, окно `Sw/StockPickForm.cs`); правило владельца — нет материала и подходящий один → молча, несколько → спросить, выбран конструктором и не совпадает → уведомление без правки. 18 юнит-тестов, живые тесты T01–T08, прогон на девяти деталях заказа NC3-7R. Изменено З-25 и З-27 (шаг 2, 059a9a5, PR #18): «молча» — только когда материала нет; замена материала, выбранного конструктором, — только ответом в окне «Проверить изделие», окно `StockPickForm` удалено |
| З-13 | 21.09.2026 | Какая длина заготовки попадает в ведомость и «Расход»: фактическая или та, по которой строился эскиз? Деталь могли удлинить бобышкой или отрезать плоскостью, а идти она может и по диагонали — габарит по осям для неё не длина. | Книга читала свойство списка вырезов по английскому имени `LENGTH` (`Sw/LzkService.cs`), а русский SolidWorks называет его «ДЛИНА» | исправлено: свойство ищется по ссылке внутри значения («LENGTH@@@…»), она английская при любом языке интерфейса (`Core/CutListProperties.cs`). На настоящей укосине NC3-7R.02: было «556×40×20\*» (оценка по габариту), стало «L=556» |
| З-14 | 21.09.2026 | «Расход» и «Сводная» ЛЗК: списание будет другим документом — убрать расчёт хлыстов, закупку, КИМ, «списание»; оставить только сколько идёт на заготовки по длине и столбец с запасом по норме (выбрали 10 % — 10 %). | `Core/LzkBook.cs` (Cost, Summary), лист «Нормы» — `Запас.Труба`/`Запас.Лист` | исправлено: 317f74b, опубликовано в 2026.09.21.2119 (локально и на NAS), юнит-тест `LzkBookTests` |
| З-15 | 21.09.2026 | В спецификацию не попадает формат листа (А4, А3, А2…): «Формат» модели не заполняется автоматически. | `Sw/DrawingFormatService.cs`, `Sw/EventHub.cs` (задача «drawingformat»), журнал надстройки: «Документ закрыт до выполнения отложенной задачи „drawingformat“» (Укосина, Царга, 778.КРВ Кровать) | исправлено: запись формата ждала простоя вместе с чертежом, а чертёж сохраняют и сразу закрывают («Сохранить?» → «Да») — задача отменялась. Теперь листы читаются при сохранении, модель находится по пути (выгружена — открывается скрыто); пустой «Формат» деталей и сборок дозаполняет «Синхронизировать» на сборке по чертежу рядом. e2e D14 (на старой версии падает), D15. Дополнение 22.09.2026 (живая проверка на копии NC3-7R): у листовых деталей формат не писался совсем — служебный лист развёртки Drew «DXF1» произвольного размера давал «лист не ГОСТ». Теперь листы DXF…, как SP/VP, не считаются; e2e D16 (на прежней версии падает). Изменение 23.09.2026 (указание владельца: деталь без команды конструктора не сохраняется): при сохранении чертежа закрытая модель больше не открывается и не сохраняется, открытая получает «Формат» без сохранения; строка состояния говорит об этом. Прежний или пустой «Формат» приводит к чертежу «Синхронизировать» на сборке (сверка, если чертёж новее модели). Старое поведение — настройка FormatSavesModel=1; e2e D13, D14, D17 |
| З-16 | 21.09.2026 | ЛЗК: главная сборка попадает на «Комплектовочный» — недопустимо, комплектуют то, из чего собирают изделие. И устранить остальные ошибки ЛЗК из аудита: двойной счёт, покраска и т. д. | `Core/LzkBook.cs` (SectionItems, KittedWhole, PaintCount), `Sw/LzkService.cs` (Describe, MarkPaintedUnits, ExcludePurchasedContents, WriteProperties), `Core/Lzk.cs` (Match) | исправлено: главная сборка с «Механической сборкой» шла на комплектовку по операции — теперь на «Комплектовочном» её нет никогда. По аудиту: размеры исполнений — по своей конфигурации (Л-1), «Раздел» «Материалы»/«Прочие» — только на комплектовку, не в прокат и не в краску (Л-2), покраска по экземплярам вне окрашиваемых узлов (Л-3), состав покупной сборки не попадает в книгу (Л-4), покраска по всем названиям (Л-5), сверка со SWTools по исполнению (Л-6), «Габарит» — в конфигурацию исполнения, одна запись на файл (Л-7). Юнит-тест, e2e L11 (на прежней версии падает) |
| З-17 | 22.09.2026 | Аппаратное ускорение снова серое («Использовать программу OpenGL» отмечена и недоступна, «Повышенная производительность» серая) — нужно, чтобы работало | `01_Настройки_SolidWorks/_Служебное/Setup_Workstation_SolidWorks.ps1` (AllowList) | исправлено: установка 21.09 снимала разделы AllowList\Gl2Shaders и AllowList\NVIDIA Corporation, которыми ускорение было возвращено; без них SolidWorks берёт для GeForce встроенную маску (Current\Workarounds = 16) и уходит на программный OpenGL. Теперь установка на NVIDIA пишет Workarounds = 0x00030408 под точной строкой рендерера из AllowList\Current\Renderer (через .NET — в имени «/»), снимает разделы под общими именами и Current не трогает — падение 20.09 было от маски с лишним битом под общими именами и перезаписи Current. Снятие «Saved OGL Settings» ускорение не возвращало (проверено: чистый запуск снова запомнил 0x02110211). Статический тест T0. ОТКАТ 22.09.2026 11:00: с маской 0x30408 под точным рендерером SolidWorks 2025 падает при старте (0xC0000005 в sldappu.dll) — как 20.09; разделы сняты, запуск восстановлен. Установщик маски снова только снимает. Аппаратное ускорение на GeForce остаётся ОТКРЫТЫМ вопросом. РЕШЕНО 22.09.2026 11:40 (подтверждено владельцем снимком параметров): настоящая причина — AllowList это собственная база видеокарт SolidWorks (~1200 разделов); установщик с 20.09 вырезал из неё ветки Gl2Shaders и NVIDIA Corporation, DATE оставался — база не восстанавливалась, RTX 2080 Ti без записи получала маску 16 и программный OpenGL. Раздел AllowList снят целиком — SolidWorks записал базу заново, маска стала 0x02501001, программный OpenGL снят, «Повышенная производительность» доступна. Установщик теперь базу по частям не правит: неполную (нет Gl2Shaders) снимает целиком, целую не трогает |
| З-18 | 22.09.2026 | «Выгрузить в производство» у заказа 778 (кровать) не делает IGS на труборез и DXF развёрток: в окне только «нет чертежа». | Трубы заказа — тела, выделенные из многотельной детали «Разделить»: нет ни элемента сварной конструкции, ни списка вырезов, а выгрузка узнавала профиль только по ним и молча пропускала. Теперь IGS — и по материалу «Труба…», «Профиль…» (то же правило, по которому ЛЗК ставит «Лазерная резка трубы»); материал «Лист…» без листового металла — замечание вместо тишины. Щиты (царга, фасады, подложка) без материала — ни DXF, ни IGS не положено. `Sw/ExportService.cs`, e2e X06 | исправлено |
| З-19 | 22.09.2026 | Обход изделия пишет «Обозначение не совпадает с именем файла и оставлено без изменений» (у тел из «Разделить» — «Тело5», «Вырез-Вытянуть3[1]») — нужен выбор: некоторые детали исправить по имени файла. | Окно выбора при обходе изделия: отметить, кому взять обозначение из имени файла; имена тел и элементов отмечены заранее. `Sw/DesignationPickForm.cs`, `Sw/BatchSyncService.cs`, `SyncRequest.DesignationFromFile` | исправлено; изменено З-27 (шаг 1, 56b3572, PR #18): окно обхода изделия удалено, «Взять из имени файла» — вопрос в окне «Проверить изделие» (`Sw/ProductReviewService.cs`); e2e T07, T08 |
| З-20 | 22.09.2026 | Материал «Труба 40х40х2» у детали с профилем 80х80х4: надстройка пишет «не соответствует геометрии … значение не изменено». Должна менять сама; предупреждать — только когда вариантов несколько и нужен выбор. | Правило 20.09 («выбор конструктора не переписывать») отменено владельцем: расхождение и один подходящий — замена молча (`StockVerdict.Replace`), несколько — окно выбора, нет в библиотеке — замечание. `Sw/StockService.cs`, юнит-тесты `StockTests`, e2e T05 | исправлено; изменено З-27 (шаг 2, 059a9a5, PR #18): замена «молча» отменена — расхождение с геометрией предлагается в окне «Проверить изделие», материал конструктора меняется только его ответом (изменение к решению 22.09.2026 записано в строке З-27) |
| З-21 | 22.09.2026 | Заказ 778: «Выгрузить в производство» не дал DXF листового кронштейна `778.КРВ.00.005`. У листовых деталей бывают исполнения — выгружаться должны все, в имени файла — обозначение, наименование, толщина и количество на изделие. | Две причины. (1) Кронштейн лежит в «Стандартные изделия и фурнитура», а всё из этой папки считалось покупным — теперь деталь с обозначением из серии изделия («778.» у шифра «778.КРВ», «ПРТИ.468211.» у «ПРТИ.468211.180») своя: в выгрузке, ЛЗК и проверке (`ProductLocator.IsOwnDesignation`). (2) Состав собирался по файлам — развёртка была бы только у активной конфигурации. Теперь считаются все экземпляры по исполнениям, каждое исполнение по очереди делается активным, имя `778.КРВ.00.005-02 Кронштейн угловой левый_S3мм_2шт_120х80.dxf`, конфигурация возвращается. `Sw/ExportService.cs`, `Core/ExportNaming.cs`, юнит-тесты, e2e X07 | исправлено |
| З-22 | 22.09.2026 | IGS на труборез должен выгружаться, если в операциях детали стоит галочка «Лазерная резка трубы». | `LzkOperations.WantsTubeFile`: операции заданы — решает только галочка (снята — IGS нет даже у трубы; стоит — IGS есть у любой детали); операций нет (ЛЗК не строилась) — прежний признак профиля в модели. `Sw/ExportService.cs`, юнит-тест, e2e X08 | исправлено |
| З-23 | 22.09.2026 | ЛЗК: у детали с двумя исполнениями в ведомости остаётся одно (L11), какое — от раза к разу разное. | Не ЕСКД: в настройках SWTools включена кнопка ленты «объединять конфигурации» (`%APPDATA%\SWTools\Settings\SWTools.settings`, `togetherConfig`, с 14:46), и выгрузка без окна склеивала исполнения одного файла. Исправлено в SWTools 1.1.113 (c07f517, PR lunin02118-ops/swtools#23, слияние 7b4574b): без окна конфигурации всегда раздельные. Группа L 11/11 при включённой кнопке. Выпуск 1.1.113 опубликован 23.09; на 25.09 на ПК разработчика стоит 1.1.110, установщика SWTools в выпуске инструментария нет — раздаёт владелец | исправлено в SWTools 1.1.113, не раздано |
| З-24 | 22.09.2026 | «Синхронизировать» не ставит исполнение из конфигурации: у детали приходится открывать MProp и отмечать галочки «Исполнение» и «Из». | Надстройка писала «Исполнение» = 2 («Исполнение» без «Из») и только в пустое. Теперь при каждой синхронизации — как MProp: «1» («Исполнение» + «Из»), когда номер — первое слово имени конфигурации; «2», когда номер у родителя («Покраска» под «01»); «0» у базовой и при номере в имени файла. У детали БЧ — только в пустое. `DesignationParser.ExecutionFlag`, `Sw/SyncService.cs`, юнит-тест, e2e M06/M06b/M06c. Уточнено 24.09.2026: «1» — только у двузначного номера; у трёхзначного («001», ГОСТ 2.201-80) — «2», иначе SaveDRW и SaveAsPDF берут из имени конфигурации «00» и не сохраняют чертёж «-001». Эталон R01: «2» → «1» у A-03 и B-02 объяснено правилом AD-30 | исправлено: e2e M06, M06b, M06c и R01 зелёные на коде с окончательным правилом 5e19dfe (прогон 24.09, 70486cb) |
| З-25 | 23.09.2026 | Деталь не должна сохраняться без команды конструктора: сохранения надстройки сделать управляемыми, петли убрать. | Аудит 23.09.2026: `Sw/EventHub.cs` (задачи «stock», «drawingformat»), `Sw/BatchSyncService.cs`, `Sw/DrawingFormatService.cs` | исправлено (PR #18, 10857b7): материал по типоразмеру ставится в открытую деталь без сохранения, замена выбора конструктора — только через окно с «Оставить как есть»; очередь простоя без дублей и с обработкой снимка; «Синхронизировать» на сборке — отказ для несохранённой, сама сборка не сохраняется, перед сохранением список и вопрос, детали с несохранёнными правками не сохраняются; «Формат» из чертежа — без сохранения модели (`FormatSavesModel=1` — как было). Юнит и статические тесты; e2e 25.09.2026 на main: D14, D17 зелёные (прогоны r64, r66); D13 — проверки прошли, но тест засчитан провалом из-за окна Drew при запуске SolidWorks («Drew не удалось создать кнопки в меню «Инструменты»»); T05 падает: у трубы, у тела которой материал уже стоит, замена материала детали материал тела не меняет, и «Материал_Строка» в файле остаётся прежним (разбор 25.09.2026, ждёт решения владельца). **Решено 26.09.2026** (решение владельца: исправить тестовую модель, код надстройки не менять): материал, назначенный до первого перестроения элемента конструкции, SolidWorks записывает ещё и телу, и через API его не сменить — тест теперь строит трубу как конструктор (элемент перестроен, потом материал), `09_Тесты/eskd_e2e/build.py`; e2e T05, T06, T09 зелёные (прогон r89, выпуск 2026.09.26.1454) |
| З-26 | 23.09.2026 | ЛЗК не должна влиять на другие заказы: модели других заказов — только в книгу. | `Sw/LzkService.cs` (запись «Операций» и «Габарита» во все модели вне базы — решение 19.09.2026) | исправлено: модель другого заказа получает операции только в книге, её файл не меняется даже в памяти (исполнения и развёртка не переключаются); другая папка своего заказа — галочка «В модель» в окне операций, по умолчанию стоит; итог «В модели / только в книгу» под таблицей, строка в замечаниях. `ProductLocator.OrderOf`, `LzkNaming.Place`; юнит-тесты, e2e L12 зелёный (25.09.2026, прогон r64 на main) |
| З-27 | 23.09.2026 | Нужен один простой порядок работы, а не вопросы в разных местах (после сохранения, в синхронизации, в ЛЗК, при выдаче). Решение: одна кнопка «Проверить изделие» — все вопросы изделия (имена, материал к профилю, формат, масса) в одном окне, выбор и «Применить и сохранить» закрывают вопрос без повторных сохранений. Ctrl+S — без окон, только подсказка в строке состояния. «Синхронизировать» на сборке убирается (на детали и чертеже остаётся). ЛЗК и выгрузка ничего не спрашивают, если изделие проверено и не менялось; иначе «Проверить сейчас / Продолжить как есть». «Готово к производству» — только при свежих проверке, ЛЗК и выгрузке. | Аудит 23.09.2026, вариант Б в новой форме: без отдельной панели справа. `Core/ProductReview.cs`, `Sw/ProductReviewService.cs`, `Sw/ProductReviewForm.cs`, `Sw/CheckService.cs` | шаг 1 исправлен (PR #18, 10857b7): «Проверить изделие» — одно окно из четырёх частей (вопросы, «обновится само», замечания, список сохранения) и кнопки «Применить и сохранить (N)» / «Применить без сохранения» / «Отмена»; файлы с несохранёнными правками конструктора — без галочки; «Оставить как есть» помнит сама модель (свойство «ЕСКД_Принято»), «Решить позже» ничего не меняет. «Синхронизировать» убрана с вкладки сборки, прежний обход изделия и окно обозначений удалены. Проверка без окна сборку больше не перестраивает и не помечает изменённой (ошибки — по списку «Что не так»). Попутно найдено: свойства, записанные надстройкой через API, не помечали документ изменённым — SolidWorks закрывал его без вопроса «сохранить?», и записанное пропадало; теперь помечают. Проверено на SolidWorks: сохранение сборки не пишет её несохранённые детали (e2e D18), разворот облегчённых компонентов сборку не меняет (K07). Юнит- и статические тесты; e2e T12, K06, K07, D18. Шаг 2 исправлен: Ctrl+S ничего не спрашивает — окно выбора материала после сохранения убрано (и параметр `StockAskOnSave`); пустой материал с одним подходящим подставляется в открытую деталь, как раньше, а замена материала конструктора и выбор из нескольких ждут ответа в окне, в строке состояния — что подходит и где ответить. **Изменение к решению 22.09.2026:** единственный подходящий материал больше не заменяет материал конструктора молча при сохранении — по З-25/З-27 замена только ответом в окне (там этот вариант первый). «Синхронизировать» в детали открывает то же окно для одной детали; e2e T03, T05. Шаг 3 исправлен: проверка пишет в `_Проверка.txt` **версию изделия** (время и сумма по файлам); повторная проверка неизменённого изделия версию не меняет. Версия считается по файлам: несохранённые правки её не меняют, а дают замечание проверки (в своих документах изделия — главная сборка и непокупные модели папки изделия). ЛЗК и выгрузка изделия сверяются с последней проверкой: совпало и несохранённых правок нет — ничего не спрашивают; иначе окно «Проверить сейчас / Продолжить как есть / Отмена» («Проверить сейчас» нет, если мешают только несохранённые правки или кнопка нажата на подсборке при проверке главной сборки; не сошлось и после проверки — вопрос повторяется). Книга (строка паспорта «Версия изделия») и `_Экспорт.txt` запоминают версию или «не проверено»; проверка даёт замечание, если книга или выгрузка сделаны по другой версии (и у книг до 23.09.2026 — «без отметки версии»), поэтому «Готово к производству» идёт только при одной текущей версии. **Отступление от плана:** свои сохранения ЛЗК и выгрузки не убраны, а ограничены — без них SolidWorks при закрытии спрашивал бы «сохранить?», а записанные «Операции» пропадали бы. Сохраняется только то, что кнопка изменила сама (в том числе модели, которые ЛЗК переключала для замера, — и при «Отмене» окна операций); документ с несохранёнными правками конструктора не сохраняется никогда (свойства в нём записаны, в замечаниях — «сохраните сами»); деталь после выгрузки — только если исполнение и временная СК трубы проверенно вернулись. Такие сохранения не запускают синхронизацию по событию и не меняют версию: новые суммы файлов вписываются в отчёт проверки («Суммы обновлены: …»). ЛЗК изменила «Лазерную резку трубы» — прежняя выгрузка отмечается «не проверено»; выгрузка одной детали с несохранёнными правками или изменённой после проверки — тоже. Ревью шага 3 (8 агентов): подтверждённое исправлено. e2e L13, L14, X09. Шаг 4 исправлен (остаток аудита 23.09.2026): ревизия детали БЧ (её «Ревизия», а не «Revision») теперь видна выгрузке — после «Новой ревизии» БЧ-деталь выгружается с `_ИзмN`, а не пропускается как выданная; «Сделать независимым» стирает «Ревизию» эталона. «Новая ревизия» по шагам: журнал → штамп (не сохранился — штамп возвращается, строка журнала помечается «ОТМЕНЕНО») → выгрузка → перенос прежних файлов в `_Аннулировано` только туда, где легли новые, и только файлов этого документа (раньше «Деталь1» уносила и «Деталь10»). «Готово к производству» пишет в `_Выдано_…` номер последней строки журнала; проверка изделия сверяет выданные детали и чертежи с этим отчётом и даёт замечание «изменён после выдачи, а новой ревизии нет» (сохранения самих кнопок вписываются и в отчёт выдачи). Материал в других исполнениях детали (решение владельца 23.09.2026: в исполнениях бывают разные материалы): где та же позиция и тот же прежний материал, сам ставится только единственный подходящий к пустому материалу — как и так поставила бы система; выбор из нескольких и замена — ответ конструктора, в другое исполнение не переносится, окно после «Применить» называет исполнения, где ответить отдельно. В многотельной детали материал трубы детали целиком больше не ставится (пластины молча становились трубой); телу по отдельности SolidWorks 2025 материал через API не ставит — надстройка это проверяет и пишет замечание «назначьте телам вручную» (найдено e2e T14; прежний код и тут считал «код 1 = ошибка», хотя 1 — «без ошибок»). «Сохранить» в окне настроек документы больше не меняет. Ревью шага 4 (15 подтверждённых замечаний) исправлено: PDF чертежа заменяется новым PDF под любым исполнением; унесённые в `_Аннулировано` файлы убираются из `_Экспорт.txt`; оставшиеся файлы прежней ревизии называются поимённо с указанием перенести их вручную (выгрузка их не уносит); строка журнала считается отменённой, если «Что изменено» начинается словом «ОТМЕНЕНО» в любом виде; журнал, открытый в Excel, читается; не прочитанный журнал — «Журнал: не прочитан» в отчёте выдачи (сверка по дате) и одно замечание проверки вместо ложных «новой ревизии нет»; метки «~$…» не сверяются; суммы файлов, сохранённых кнопкой в папке другого изделия, вписываются и в его отчёты; «Ревизия» модели читается только у детали БЧ; производные конфигурации не считаются исполнениями; материал только у тел — замечание про материал детали целиком (графа 3, ЛЗК); без списка вырезов материал профиля ставится, только если профиль — вся деталь; позиция в порядке, только если материал у всех её тел. Открытыми остаются: EXP-3 (ревизия детали с исполнениями выгружает только активное исполнение и без количества в имени DXF; прежние файлы остальных исполнений остаются на месте и называются поимённо — перенести вручную), MAT-15 (у детали из нескольких профилей «Материал_Строка» показывает один материал или пропадает) и проверка толщины листовых тел в смешанной детали (часть MAT-6). Юнит IssueRecordTests, ProductStampTests; e2e V05, V06, V07, T13, T14 |
| З-28 | 23.09.2026 | Развёртки брать по границе внешней рамки: радиусы и дуги учитывать, чтобы размер заготовки в DXF не получился неверным. | `Core/DxfFrame.cs` (замер рамки по DXF), `Core/ExportNaming.cs` (округление), `Sw/LzkService.cs` («Габарит» развёртки без свойств граничной рамки) | исправлено (PR #18, 10857b7): рамка в имени DXF — граничная рамка развёртки, как её считает SolidWorks: наименьший прямоугольник по внешнему контуру с дугами, окружностями, сплайнами, эллипсами, дугами полилиний и блоками (раньше у дуги бралась точка центра, у сплайна и эллипса — векторы как точки, рамка — по осям чертежа). Проверено на SolidWorks 2025: диск Ø150 — `150х150` (раньше DXF не делался: «развёртка пустая»), планка с полукруглыми торцами — `80х280` (было `80х200`), пластина 200×100 под 30° — `100х200` (было `187х223`), косынка 200×100 — `100х200`; всё совпадает с «Длиной/Шириной граничной рамки» SolidWorks. Стороны округляются вверх, как и было задумано (было — до ближайшего: 200,4 мм давали `200`). ЛЗК у детали без свойств граничной рамки меряет развёртку по телу тем же способом (раньше — габаритный ящик по осям модели: `223×187`), развёрнутую конструктором деталь после замера не сворачивает (раньше сворачивала). У гнутой детали SolidWorks отдаёт кривую ребра развёрнутого тела от другого начала: уголок с полками 100 и 60 мерился 262 мм вместо 161 — прямые рёбра теперь берутся по концам, точки кривых сверяются с концами ребра. Юнит ExportNamingTests, EdgePointsTests; e2e X11, L15 |
| З-29 | 23.09.2026 | Сверить код надстройки с практиками SolidWorks API и исправить найденное. | Сверка 23.09.2026 (43 находки) | исправлено (PR #18 и PR #21): IGS трубы выгружается для каждого исполнения из изделия, со своим обозначением, — раньше только для активного в файле, и исполнение, не активное в файле, цех не получал (находка 12; e2e X10, X12); ось IGS — по элементу конструкции своего исполнения, погашенный не в счёт (e2e X13); DXF выгружается в версии R2000 и масштабе 1:1 по Т-28, а не как настроено у конструктора, настройки после выгрузки возвращаются (находка 22); рамка развёртки — З-28 (находка 13); прежний DXF детали с другим именем (рамка, количество, толщина) уходит в `_Аннулировано` (e2e X14); проверка изделия сверяет материал каждого исполнения, стоящего в изделии (находка 14; e2e K08); ЛЗК меряет развёртку своих моделей по телу всегда — свойства списка вырезов могли устареть, — по всем рёбрам, а не одной грани, и не мерит не перестроившуюся развёртку (ревью; e2e L15); сбой одного документа не обрывает выгрузку — остальное выгружается, отчёт пишется, сбойный — в «Пропущено» (находка 29; e2e X16); деталь без модели в памяти названа в «Пропущено»: «модель не загружена» (находка 20; e2e X17, X18). Вторая часть: кнопка «Новая ревизия» узнаёт о выдаче в фоне, SolidWorks не ждёт сетевую папку при перерисовке вкладки (находка 1; юнит BackgroundFlagsTests, e2e V08); надстройка при сбое загрузки и при выгрузке снимает всё, что поставила, ведомость ЛЗК прерывается с сообщением, прежняя книга на месте (находки 0, 3; e2e I11, L17); закрытое окно детали, оставшейся в сборке, не выпадает из синхронизации (находка 9; e2e C08, P17, P18); при открытии проверяется только открытый документ и в простое (находка 11; e2e C07, P19); выгрузка и ревизия закрывают окна, которые открыли сами (находка 15; e2e X19, V09); «Закрыть заказ» останавливается, если открыт одноимённый документ другого заказа, и не собирает комплект с подставленными файлами (находка 16; юнит OrderArchiveTests, e2e Y06); свой материал тела учитывается в графе 3, «Материал_Строка», БЧ и проверке, у тел разных материалов — замечание (находки 31, 33; юнит BodyMaterialsTests); материал телам позиции — всем или ни одному, причина отказа словами (находка 38; юнит BodyAssignmentTests); листовая деталь с телами разной толщины — замечание вместо подбора по первой толщине (находка 39; юнит StockTests, e2e T19); пустые папки списка вырезов и папки других исполнений не в счёт, подсварки обходятся, развёртка отдаёт материал родителю (находки 32, 34, 40, 41; e2e T16–T18, L16); число из текста ссылки не длина (находка 35); регистрация отказывает под чужой учётной записью (находка 2), в 32-битном PowerShell (находка 4), имя и версия сборки — из DLL (находка 6); сбой кнопки — окно и журнал (находка 7); AssemblyResolve отвечает только своим сборкам (находка 8); настройки PDF, IGES, DXF возвращаются только изменённые (находка 25); «Сделать независимым» не перезаписывает чертёж (находка 27); чертежи в выгрузке — только для чтения, причины отказа открытия и сохранения словами (находки 17, 28); ЛЗК: не открывшееся исполнение — «оценка», развёрнутую деталь не сворачивает (находки 18, 19), размер развёртки своей модели, которую не удалось измерить по телу, — из свойств списка вырезов с пометкой «оценка» (сверка документации 24.09.2026; юнит FlatSizeTests); время перестроения штампа пишется в журнал (находка 30, только замер); скрытый незагруженный компонент (SolidWorks отдаёт его погашенным, но с IsLoaded = false) выгрузка, проверка и ЛЗК называют, а не отбрасывают (находка 20; e2e X17); чертежи выгрузка открывает без окна (находка 28, шаг 2; e2e X20); Ctrl+S библиотечной детали B-01 с сотней исполнений длился 31,7 с против 0,17 с без надстройки: чтение сырого значения свойства пересчитывало массу всех исполнений — теперь читается без пересчёта (находка 24; e2e P20); развёртка, созданная при прежнем материале исполнения, держит его копию, и SolidWorks за исполнением её не ведёт — такая копия получает новый материал вместе с исполнением, другой материал развёртки остаётся с замечанием (находка 40; e2e T18); окно изменённой модели выгрузка и ревизия не закрывают: CloseDoc снимает с модели, оставшейся в сборке или чертеже, отметку «изменена», и правка молча пропала бы (проба; e2e V09). Ревью этих правок (8 агентов, 9 подтверждённых замечаний) исправлено: вставший материал детали считается назначенным, перекрывающие тела — замечанием; многотельный лист с уже назначенными материалами не даёт замечания; материал тел читается только у активного исполнения; деталь без окна материал в простое не получает; отказ «Закрыть заказ» — с пределом и отдельной строкой про детали сборок; ведомость ЛЗК при выгрузке ждёт завершения SWTools; владелец сеанса при регистрации — по сеансу Windows; отказ unregister.ps1 — своим текстом (юнит, статические тесты). Проверка на SolidWorks — после освобождения стенда. Находка 10 — только документация; 21 — не подтвердилась. Решения владельца 24.09.2026: 42 — DXF на каждое листовое тело («_тело1», «_тело2») с замечанием в отчёте; 36 — «Проверить изделие» сверяет материал с профилем и в исполнениях, стоящих в изделии, а про активное исполнение вне изделия не спрашивает; 33 (графа 3) — у детали один материал, всегда материал детали; 22 — кривые в DXF как есть; 3 — прерывание ЛЗК при закрытии SolidWorks подтверждено; 5 — панель ЕСКД прятать при каждом запуске, как сейчас; 2 — установщик не трогать; 26 — менять только текст сообщения; 0 — служебный ключ DebugFailConnect оставить; 23 — порядок свойств всегда одинаковый, эталонный тест R01 разрешено пересогласовать; 16 — проверку одноимённых документов других заказов в «Проверить изделие», выгрузке и ЛЗК сделать, как правильно; модель после «Новой ревизии» — как сейчас (формат записан, конструктор сохраняет модель сам). 24.09.2026 сделано по этим решениям: 42 — DXF на каждое листовое тело (`…_тело1_S3мм_…`), в отчёте замечание; 36 — материал сверяется в исполнениях, стоящих в изделии; 26 — текст сообщения; 23 — порядок свойств один (словарь SWPlus, служебные, свои — в порядке заведения), наводится только при сохранении конструктором, изменённое текстовое свойство пишется на своей строке; 16 — проверка называет подмену, выгрузка и ЛЗК останавливаются до записи файлов (e2e Y07); 37 — только замер. Ctrl+S детали B-01: 5,7 → 1,4 с — значения свойств читаются без пересчёта массы (CustomInfo2, контракт C11; e2e P20); запись текста поверх числового свойства SolidWorks принимает (код 0), но свойство теряет — числа переписываются удалением и добавлением (контракты C09, C10). PDF из чертежа без окна SolidWorks 2025 не пишет: окно чертежа показывается только на время записи PDF (e2e X20); после выгрузки снова активна сборка изделия (e2e X19). «Закрыть заказ»: главная сборка — по шифру папки изделия, раньше у `ПРТИ.468211.100` в комплект уходила подсборка 108 (юнит OrderArchiveTests, e2e Y06); Pack and Go SolidWorks 2025 SP3 перечисляет только сборку и чертежи, и комплект уходил в архив без деталей — сборка из архива открывалась пустой; теперь комплект дополняется всеми файлами, от которых зависит сборка, и сверяется с папкой (юнит OrderArchiveTests, e2e Y01, Y06). Эталон R01 пересогласован так же, как в ветке `fix/r01-ispolnenie` (правки взяты дословно, слияние без конфликта): правило AD-30 (З-24, «Исполнение» = «1»), R01 сам включает события документа в зонде — отдельно он падал с 21.09 и на main. Прицельные e2e зелёные после перезапусков: в r34 падали X19 и X20, в r35 — R01 и Y06; все прошли в r36 и r37. |
| З-30 | 22.09.2026 | Заказ 778: новый чертёж из шаблона `Чертеж.drwdot` — формат листа не находится, лист пустой. | Шаблон хранит путь к основным надписям `…\Макросы_SW_ZTool\SWPlusMacro_v_2018_SP0.0\Основные надписи\`, которого нет; настоящие — в `02_Шаблоны_и_Форматки\Основные надписи` (Master.ini, строка 4). Тот же путь у листа DRW1 чертежа Кровати. При создании через API — `SetupSheet5` с настоящим путём. Нужно: перевыпустить шаблон с верным путём (шаблон владелец правит сам) | открыто |
| З-31 | 22.09.2026 | Чертёж через API: SolidWorks сам меняет масштаб листа при вставке первого вида (1:2 → 1:10). | Настройка SW «автомасштаб новых видов». Масштаб листа задаётся после вставки первого вида (`ISheet.SetScale`, `UseSheetScale=1`). Учесть в скриптах и в скилле оформления | учтено в скриптах |
| З-32 | 22.09.2026 | Выгрузка SWTools/ЕСКД в пакетном режиме: SolidWorks молча падает после десятков открытий-закрытий. | Задачи простоя надстроек гоняются с внешними вызовами. Помогает `ISldWorks.CommandInProgress = True` на время пакета и ожидание `PendingIdleTasks` надстройки ЕСКД перед `CloseDoc` (R01). Нужно: то же в пакетных командах надстройки | открыто |
| З-33 | 22.09.2026 | Нет связи «операции ЛЗК → профиль ТТ»: при оформлении детали ТТ выбирается вручную. | В `TT_Prof.txt` нет профиля лазерного трубореза (есть «Сортовой прокат / Труба (отрезка в размер)»). Для 778 ТТ нанесены через `apply_tt_profile.py` с профилем «Деталь: Лазерная резка трубы» (5 пунктов). Нужно: профиль в `TT_Prof.txt` и выбор профиля по свойству «Операции» | открыто |
| З-34 | 22.09.2026 | Переименование сборки не обновляет «Наименование_ФБ»: у 02.000 Изголовье в основной надписи осталось бы «Изножье». | Свойство для основной надписи MProp пишет отдельно от «Наименования». Исправлено вручную у 778.КРВ.02.000; у Лежака общее «Обозначение» было `778.01.000` — исправлено. Нужно: «Синхронизировать» сверяет *_ФБ с исходными свойствами | открыто |
| З-35 | 22.09.2026 | Переименование подсборок сбило позиции в спецификации Кровати: Изголовье стало позицией 19 вместо 2. | BOM считает переименованный файл новым компонентом и даёт ему следующий номер. Чертёж Кровати владелец ведёт сам — не тронут. Нужно: перенумеровать BOM (SpecEditor) после переименования | открыто |
| З-36 | 22.09.2026 | У Кронштейна 778.КРВ.03.005 свойство «Формат» = А3 во всех исполнениях, а чертёж — А4. | В спецификациях подсборок графа «Формат» показывает А3. Проверить, откуда `DrawingFormatService` берёт А3 (служебный лист развёртки?) | открыто |
| З-37 | 22.09.2026 | Библиотека проектирования: у крепежа условная резьба была погашена или с неверными размерами (26 деталей). | Исправлено: включены, диаметр и длина по ГОСТ/DIN, обозначение; у Заклепки М8 резьба добавлена. Не исправлено: шпилька колеса М10 (импортированная геометрия без цилиндров); внешние ссылки на личные пути `Downloads\Telegram Desktop`, `E:\Саша\…`, `D:\Work\Заявки\85_Т` у опор и заглушек; у 7985 M4x12, 7380-1 M8x12, 7380-2 нет обозначения резьбы | частично |
| З-38 | 22.09.2026 | Скрипт `apply_tt_profile.py` всегда ставит пункт «*Размеры для справок», даже если справочных размеров нет. | ГОСТ 2.316: пункт нужен, только когда на чертеже есть размеры со «*». В чертежах 778 такие размеры есть (сечение профиля, толщина рамы). Нужно: проверять наличие «*»-размеров | открыто |
| З-39 | 22.09.2026 | Лист, добавленный к чертежу через API, оставляет в основной надписи «Листов 1» — на групповом чертеже Кронштейна было два листа. | «Листов N» — это не свойство, а текст заметки в основной надписи (аннотация `Sheet2` первого вида); его пишет макрос DProp при своей работе. Скриптом правится `INote.SetText("Листов %d")` (`set_total_sheets.py`). Нужно: листы, добавленные не DProp, тоже обновляют графу | учтено в скриптах |
| З-40 | 22.09.2026 | Таблица (швов, исполнений), вставленная через API, не видна на листе и не выбирается по имени. | `InsertTableAnnotation2` в API SW2025 нет — только `IModelDocExtension.InsertGeneralTableAnnotation`, и она кладёт таблицу за пределами листа: положение задаётся отдельно (`IAnnotation.SetPosition2`), а выбрать таблицу можно только по точке (`SelectByID2("", "TABLEANNOTATION", x, y, …)`) и только после переноса на лист. Наружный контур у новой таблицы — утолщённая линия (`BorderLineWeight` = 4): по ГОСТ 2.105 ставится основная, `BorderLineWeight` = 1, `GridLineWeight` = 0 | учтено в скриптах |
| З-41 | 22.09.2026 | Знак «по замкнутой линии» (кружок на полке) у обозначения шва средствами API не наносится. | У заметки `IAnnotation.SetLeader3(…, AllAround=True)` игнорируется, а окружность эскизом не видна: у чертежей заказа отключено отображение эскизов (`swDisplaySketches` = False). Обошли символом «○» в тексте заметки. Нужно: проверить, даёт ли кружок вставка обозначения шва через `InsertWeldSymbol` | обходной путь |
| З-42 | 22.09.2026 | Изголовье 778.КРВ.02.000: правый кронштейн не прилегает к стойке — зазор около 3 мм, левый прилегает. | Найдено при расстановке швов по касанию габаритов компонентов. Сварить такой стык по чертежу нельзя. Модель владелец правит сам | открыто |
| З-43 | 22.09.2026 | Кронштейн 778.КРВ.03.005: в параметрах листового металла радиус гиба R6, а оба гиба («Ребро-кромка») построены с R2,5 при толщине 3 мм. | ТТ группового чертежа приведены к модели («Радиусы гибов R2,5»). Но R2,5 меньше толщины: для Ст3сп 3 мм минимальный внутренний радиус гиба ≈ 1×S = 3 мм — на гибе возможны трещины. Нужно решение владельца: править модель или оставить | открыто |
| З-44 | 22.09.2026 | Прежний чертёж Кронштейна был выполнен в масштабе 1:1,5 — такого масштаба в ГОСТ 2.302 нет. | Чертёж заменён групповым (ГОСТ 2.113) в масштабе 1:1, старый перенесён в `_Аннулировано`. Нужно: проверка масштаба листа по ряду ГОСТ 2.302 при проверке изделия | открыто |
| З-45 | 23.09.2026 | Чертёж листовой детали: на развёртке не было размеров положения линий гиба — гибщику не от чего отмерять. | Линия гиба в виде развёртки — не ребро, а эскиз внутри вида: `IView.GetBendLines` её отдаёт, `SelectByID2("", "BENDLINE", …)` выбирает, но размер к ней API не создаёт (`AddHorizontalDimension2` возвращает None, «умный» размер меряет только кромку). Обошли так: в эскизе вида рисуется вспомогательная линия поверх линии гиба и помечается конструктивной (не печатается), размер ставится к ней (`sm_group_drw.dim_bends`). Координаты эскиза вида = координаты листа минус `IView.Position` | обходной путь |
| З-46 | 23.09.2026 | Проекционный вид (вид сверху) уезжал за рамку листа. | Выравнивание проекционного вида идёт по началу координат родителя, а не по геометрии: деталь смоделирована в координатах сборки (X ≈ −2 м), поэтому геометрия и точка привязки разнесены на 75 мм. Решение: `IView.RemoveAlignment()` и постановка вида по центру контура; точка вставки проекционного вида считается от `GetOutline`, а не от `Position` | учтено в скриптах |
| З-47 | 23.09.2026 | Детали из трубы с резом под 45°: на чертеже был один размер длины — непонятно, по какой стороне резать. | Косой рез даёт разные длины сторон (Стойка 778.КРВ.02.001: 800 и 765,4). Теперь ставится оба размера: по длинной стороне (над видом) и по короткой (под видом), `tube_drw.run` | исправлено |
| З-48 | 24.09.2026 | Повторная выгрузка изделия (вторая итерация опытного образца, правки после выдачи): прежние развёртки и PDF деталей, которые убрали из сборки или переименовали, оставались в папках выдачи, и «Готово к производству» отдало бы их цеху вместе с изделием. Нужно, чтобы всё было проверено перед выдачей и максимально автоматически. Там же: выданная «_Изм1» после повторной выдачи не была защищена от перезаписи. | `Sw/ExportService.cs` (выгрузка убирала только прежнюю развёртку под тем же обозначением; защищала только ревизию 0), `Sw/CheckService.cs` (правило «е» сверяло только файлы из отчёта), `Sw/ReadyService.cs` (отчёт выдачи — все файлы папок) | исправлено: выгрузка с главной сборки оставляет в папках выдачи выгруженное сейчас и выданное цеху, прежние и чужие файлы — в «_Аннулировано» с замечанием (`Core/ExportNaming.cs`, `ExportLeftovers`); выданная ревизия N защищена, её файлы уносит выгрузка ревизии N+1; проверка называет файл папки выдачи, которого нет в отчёте выгрузки, и выданный файл документа, убранного из изделия без новой ревизии сборки (e2e X22, X23, K09; юнит ExportNamingTests)
| З-49 | 24.09.2026 | Повторная «Выгрузить в производство» (старая установленная версия): в IGS для трубореза выгрузилась вся сборка целиком, все детали, а не каждая деталь отдельно. Нужно: у каждой детали, отмеченной в ЛЗК на труборез, свой IGS только с её телом, развёртки DXF каждой листовой детали — при первой и повторной выгрузке, для всех исполнений, которые стоят в изделии. | `Sw/ExportService.cs` `IgsOne`: IGS сохранялся из модели детали, которую выгрузка не делала активной; SolidWorks пишет в IGS активный документ (вся сборка после PDF её чертежа или последняя развёрнутая пластина), а заголовок файла называет деталь. Так — у трубы без элемента конструкции (вытянутой, выделенной из многотельной, с галочкой «Лазерная резка трубы») и у исполнения, активного в файле; что попадало в файл, зависело от порядка обхода и открытых окон. Воспроизведено на коде main (прогон r38: X06, X24, X25). Ещё две причины нашлись при проверке: ЛЗК предлагала «Лазерная резка трубы» только по `Материал_Строка` — у заказа, ещё не оформленного по ЕСКД, труба получала одну «Покраску», и выгрузка после ЛЗК IGS не делала (L18); открыв окно детали, SolidWorks 0,5–4 с перестраивает дерево в фоне, обход дерева любого документа в это время даёт RPC_E_SERVERFAULT, и выгрузка принимала сбой за «не листовая» — DXF повторной выгрузки молча не делался (X24; проба r41–r44: пауза с повтором помогает всегда) | исправлено: деталь перед IGS всегда делается активной, перед записью это проверяется ещё раз; записанный IGS проверяется сам (`Core/IgesContent.cs`: подфигуры сборки 308/408 — файл удаляется, пропуск «в IGS попала не эта деталь»); проверка содержимого IGS в тестах (`eskd_e2e/iges.py`) — e2e X01, X06, X08, X10 (усилены), X24 (три выгрузки подряд), X25 (исполнения трубы), X26 (развёртки исполнений), L18 (строки ЛЗК ↔ файлы трубореза); юнит IgesContentTests; `Sw/LzkService.cs` — пока `Материал_Строка` пуст, резка предлагается по материалу SolidWorks; выгрузка не пропускает трубу без «Лазерная резка трубы» молча — замечание в отчёте (X08); `Sw/FeatureWalk.cs` — обход дерева на пути выгрузки повторяется после паузы (юнит FeatureWalkTests); X24 сверяет файлы с отчётом своей выгрузки и требует пустое «Пропущено». Выданные раньше IGS защищены выдачей — их проверяет владелец или новая ревизия |
| З-50 | 24.09.2026 | Порядок свойств: «Применить» MProp уносит «Примечание», «Формат», «Раздел» в конец, следующее сохранение возвращало их на место — и так по кругу. «Сделай, чтобы он их не менял местами… чтобы один раз как было, и она не менялась». Свои свойства — в порядке заведения. Тот же порядок — в шаблонах детали и сборки. | `Core/PropertyOrder.cs` (единый порядок ставил их на строки словаря 7, 8, 12), MProp `FrmMProp.frm.txt` 3021–3053 (удаляет и дописывает их в конец), шаблоны `02_Шаблоны_и_Форматки/Шаблоны документов` | исправлено: «Примечание», «Формат», «Раздел» — всегда последние, в порядке MProp (`PropertyOrder.Tail`), свои свойства — перед ними в порядке заведения; MProp и сохранение больше не переставляют их (e2e M23, P25; юнит PropertyOrderTests; статический T0_property_order_tail_is_mprop_apply_order); шаблоны детали и сборки пересохранены в едином порядке (`tools/normalize_templates.py`, статический T0_document_templates_in_single_order) |
| З-51 | 24.09.2026 | Многотельная деталь вообще не должна попадать в IGS: высвечивать предупреждение, что деталь многотельная, — конструктор проверяет чертёж, сборку деталей. DXF листовых тел — как прежде, на каждое тело (№42). | `Sw/ExportService.cs` `IgsOne` (решала только галочка «Лазерная резка трубы», число тел не смотрела: «Рама сварная» фикстуры из трёх труб уходила одним IGS — e2e X01, X24, L18 это и проверяли), `Sw/CheckService.cs` правило «е» (замечаний выгрузки, кроме выданного файла убранного документа, не показывала) | исправлено: выгрузка считает тела показанного исполнения — твёрдые и поверхности, скрытые тоже (`Bodies`); многотельной IGS не делает, в отчёте и окне итога — замечание «многотельная деталь в IGS не идёт (тел N…): проверьте чертёж и сборку…» (`ExportLog.MultibodyReason`, подсказка «Проверьте чертёж и сборку детали»), прежний IGS — в «_Аннулировано»; без «Лазерная резка трубы» — только предупреждение, без совета её поставить; с ней — замечание правила «е» в «Проверить изделие» (`ExportLog.MultibodyNoIgs`), «Готово» не выдаёт. Однотельное исполнение той же детали IGS получает. ЛЗК не менялась: резку трубы детали она оставляет. e2e X27 (исполнения 2 тела / 1 тело, прежний IGS, выгрузка одной детали), K10 (замечание проверки и как его снять); X01, X24, L18 — рама без IGS с замечанием; G05 — у рамы снята резка трубы; юнит ExportNamingTests Test_Multibody_note_is_read_back_by_check. После ревью: выгрузка одной детали больше не стирает замечание о другом исполнении той же детали (`ExportLog.CarriesOver`, в отчёте метка «файл [исполнение]»), IGS, который не удалось убрать в «_Аннулировано», не возвращается в отчёт как выданный (`ExportLog.Unretired`), имя файла с « — » не ломает разбор замечания (`ExportLog.SplitNote`); юнит Test_Single_part_export_keeps_notes_of_other_executions; e2e X27 (выгрузка одной детали в другом исполнении), K09, K10, V02, V05, V09, X09, X14, X23 |
| З-52 | 24.09.2026 | Нужно использовать установщик Drew, который на этой машине (сборка без слёта лицензии, 24.09). | `03_Макросы_и_Плагины/Drw_System_Automation/УСТАНОВЩИК_Drew_AUTO.exe` — в инструментарии лежала сборка 15.09 (SHA-256 0097761…); `Setup_Workstation_SolidWorks.ps1` сверял Drew с замером 15.09, поэтому галочка «Drew» удаляла новую сборку и ставила старую; после установки новым установщиком итог сообщал «файлы Drew не обновлены» — установщик Windows оставляет файлу дату сборки | исправлено: в инструментарии установщик этой машины (SHA-256 6BD50418…, ветка drew/anti-drop-license), замер `$licHash` обновлён по чистой установке 24.09 (`CADBooster.Common.Licensing.dll` 0D31E06D…), свежесть установки — и по дате создания файла; руководство §1.5; T0 test_T0_drew_installed_with_builtin_license |
| З-53 | 25.09.2026 | Добавить выбор языка при развёртывании: на машинах часто стоит английский, а надо переключить на русский. | Окно настройки и `Setup_Workstation_SolidWorks.ps1`: была только галочка «Русский интерфейс Drew» (`DREW_LANG`); язык SolidWorks установщик не трогал. SolidWorks 2025 берёт язык не из языка Windows, а из регионального формата пользователя (`sldutu.dll` LangUtils::GetLangSubdir): «Use English language» = 1 — английский, иначе русский только при формате ru-RU / ru-MD и установленном русском пакете (`lang\russian`); у ru-KZ, ru-UA, ru-BY, kk-KZ — английский | сделано: в окне «Язык интерфейса SolidWorks и Drew: Русский / Английский» (прежний выбор ПК, иначе русский), ключ установщика `-Language` (`-DrewRussian` — синоним), шаг [9/9]: «Use English language» пишется после профиля .reg; для русского при другом формате — `Set-Culture ru-RU` (только у этого пользователя, без прав администратора), без русского пакета — предупреждение с порядком доустановки; `DREW_LANG` = ru / en; системную кодовую страницу для макросов SWPlus (1251) только проверяет и предупреждает; выбор — в `ESKD_Install\Language` (`Get-EskdLanguagePlan`); руководство §1.3, §1.4; T0 test_T0_language_choice_at_deploy, песочница check_deploy_engine.ps1; 25.09.2026 (замечание владельца: выбран русский, а SolidWorks остался английским, итог — зелёное «Готово»): русский интерфейс не включится — итог жёлтый, код выхода 4, причина в `ESKD_Install\LanguageIssue` и в строке [ВНИМАНИЕ] шага [9/9]; русский язык SolidWorks — `lang\russian\sldresu.dll` той же версии, что `SLDWORKS.exe` (прежде — любая DLL), нет `SLDWORKS.exe` — «SolidWorks не найден»; запуск под чужой учётной записью — отказ до изменений (код 1); подсказка в окне «Изменить → Языки → Русский» (PR #40); T0 test_T0_language_not_switched_is_not_green, test_T0_setup_refuses_foreign_account. Почему на машинах владельца SolidWorks остаётся английским — ждёт журнала установки с такой машины |
| З-54 | 25.09.2026 | Ревью 24.09.2026, три мелких недочёта. (1) «Отучение SolidWorks от сети» под администратором всегда заканчивалось [OK], даже если пакет упал («hosts недоступен для записи», правило брандмауэра не создано). (2) Установщик ждал правил не столько, сколько создаёт пакет: SLDWORKS.exe пропускался только наружу, а пакет с 20.09 не блокирует его в обе стороны; в Windows PowerShell 5.1 ожидание к тому же выходило 0 — ветка с запросом прав всегда писала «завершилось не полностью». (3) Групповой чертёж листовой детали без папки изделия (`ESKD_DRW_ROOT`) падал с непонятной ошибкой вместо отказа, обещанного в SKILL.md. | `Setup_Workstation_SolidWorks.ps1` шаг [8/9], `EskdDeploy.psm1` (`Get-EskdSwBlockExpectedRules`), `SwInternetBlock/Set-SwInternetBlock.ps1` (`Test-SldWorksConflict`), `eskd-drawings/scripts/tube_drw.py`, `sm_group_drw.py` | исправлено: код выхода пакета читается, сбой или нехватка правил — [ВНИМАНИЕ] «завершилось с ошибкой»; решение владельца 25.09.2026 — это примечание, установка ошибкой не считается. Ожидание — по тому же правилу, что в пакете (на этом ПК 302 правила из 304 записей). `tube_drw.require_root()`: `sm_group_drw` без папки изделия или модели отказывает до обращения к SolidWorks. Тесты T0 `test_T0_sw_block_expected_matches_package`, `test_T0_drawing_scripts_refuse_without_product_folder` (на прежней версии падают) |
| З-55 | 25.09.2026 | «Всё сохранил и обновил, выгрузил ЛЗК» — а выгрузка NC3-7R: «Изделие всё ещё не сходится с проверкой — есть несохранённые правки: NC3-7R.02.003 Закладная высоты, NC3-7R.04.002 Закладная царги, NC3-7R.03.002 Фланец». Почему так? | Не ЛЗК: после неё детали чистые (журнал 08:19:55 — изменены только сборки). SolidWorks отмечает листовую деталь изменённой при первом открытии её чертежа с видом развёртки, если деталь меняли после сохранения чертежа (перестраивает конфигурацию «…SM-FLAT-PATTERN»; воспроизведено на копии заказа и без надстройки). `Sw/CheckService.cs` правило «з» (оборванные размеры) и «Формат» по чертежу открывают чертежи — проверка сама делала детали «изменёнными» и тут же писала про них «несохранённые правки»; так же не прошло бы и «Готово к производству». `Sw/ExportService.cs` запоминала, чистая ли деталь, уже после открытия чертежа ради PDF и потому её не пересохраняла; перестроение главной сборки после сохранённой подсборки с двумя исполнениями в изделии («Укосина» 00/01) снова отмечало подсборку | исправлено: проверка в конце сохраняет своим сохранением документы изделия, чистые до неё и не оставленные окном несохранёнными, — сначала все перестроения, потом детали и сборки снизу вверх, до двух кругов (`ToolSaves.ResaveUnchanged`); ЛЗК — так же после SWTools (на свежей копии NC3-7R «Укосину» отмечало чтение после сохранённой в ней детали); их суммы — в прежний отчёт и выдачу (`ProductFreshness.Restamp`), версия изделия не меняется; правки конструктора не сохраняются (З-25). Выгрузка берёт снимок «были ли правки» до первого чертежа (`Item.Edited`), выданную пропущенную деталь после чертежа сохраняет снова (`ResaveAfterDrawing`), сборки — сначала перестроение сверху вниз, потом сохранения. На копии NC3-7R цепочка проверка → ЛЗК → проверка → выгрузка → проверка без изменённых документов, версия одна. e2e L19 (на прежней надстройке падает) |
| З-56 | 26.09.2026 | Научить установщик переставлять Drew, когда в инструментарии сменился установщик Drew: новый `УСТАНОВЩИК_Drew_AUTO.exe` лежит в общей папке, а на машинах со старым Drew остаётся старый. | Шаг [7/9] считал Drew верным, если хэш `CADBooster.Common.Licensing.dll` совпал с замером; у новых установщиков эта сборка та же, поэтому Drew не переставлялся. Отпечаток установщика (`ESKD_Install\DrewInstaller`) сравнивался только при несовпавшем хэше. | исправлено: правило `Get-EskdDrewPlan` — установщик последней подтверждённой установки на ПК (записи «время_хэш»: метки на весь ПК `%ProgramData%\ESKD\DrewInstaller` и отпечаток учётной записи с именем ПК, запись новее прежних — `New-EskdDrewStamp`, `Get-EskdDrewRecordedInstaller`) совпал с установщиком в общей папке — Drew не трогается; не совпал (новый или возвращённый прежний установщик) — Drew переставляется один раз на ПК (установщик копируется во `%TEMP%`, затем удаление и установка). Сбой удаления рабочего Drew — предупреждение (Drew цел), у чужой сборки — ошибка; неудача установки после удаления (и 3010) — ошибка. Установка засчитывается только по появлению заново удалённой сборки лицензии (код выхода AUTO.exe всегда 0, даты файлов — даты сборки); прежние файлы не удалились — «перезагрузите и повторите», отпечаток не пишется. `-Mode Uninstall` сохраняет отпечаток; руководство §1.5; T0 test_T0_drew_installed_with_builtin_license, случаи в check_deploy_engine.ps1 (test_T0_deploy_from_readonly_toolkit_folder) |
| З-57 | 26.09.2026 | Аудит «Отучения SolidWorks от сети»: нужен универсальный скрипт для любой машины, чтобы никакая телеметрия и никакие данные SolidWorks не уходили в интернет (корпоративная безопасность). «Поделиться настройками» Drew — пусть не работает; SWTools интернет не нужен, обновления — вручную; сервер лицензий на этом же ПК. Уточнение: активация SWTools на другом ПК через интернет (ввести ключ, получить ответ) должна работать. | Прежний пакет: список 304 путей с одной машины (SW 2025 SP3, диск C:, английские папки), `SLDWORKS.exe` не закрыт (основной процесс с данными, Drew с Mixpanel/OpenTelemetry внутри), не закрыты проверка обновлений и фоновая загрузка («Менеджер установки SOLIDWORKS»), на чужом ПК — «0 из 0 [OK]», правила «Internet» пропускали прокси в локальной сети, hosts искал домены подстрокой. Сделано: `SwInternetBlock/Set-SwInternetBlock.ps1` находит программы сам (реестр SolidWorks и Менеджера установки, «Программы и компоненты», Common Files, службы лицензий, надстройки SolidWorks), закрывает всё, включая `SLDWORKS.exe`, явными диапазонами интернета, прокси в локальной сети закрыт; проверка брандмауэра и политики домена; hosts по строкам; шаг [8/9] — проверка без прав, права только при нехватке; галочка по умолчанию включена; сетевые функции SolidWorks выключены в реестре; `-Mode Check` показывает состояние. Исключение: `SWTools.exe` открыт только сервер лицензий `license.vizbuka.ru` (сетевой код лицензии только в нём: активация, аренда, перенос). Руководство, раздел 1.6. Тест T0 `test_T0_sw_internet_block_universal`. Проверка перед слиянием (26.09.2026): окно администратора (UAC с паролем ИТ) брало прокси и сервер лицензий администратора — теперь конструктора по SID (`-UserSid`, раздел HKEY_USERS); программа в чужом профиле без доступа считалась удалённой (вечный запрос UAC) — теперь существующей; системный прокси на самом ПК (127.0.0.1, VPN) пропускался молча — теперь ОШИБКА и код 4; прокси на сервере лицензий больше не закрывается; окно помнит снятую галочку (`ESKD_Install\SwInternetBlock` = 0) и «Безопасную графику» (`ESKD_Install\Graphics`); запись списка папок для окна администратора — без WriteAllLines (страж файлов SWPlus). | исправлено |
| З-58 | 26.09.2026 | Скрытая ловушка установщика Drew: при установке «для одного пользователя» (без прав администратора) он сразу же стирал только что поставленный Drew. | `03_Макросы_и_Плагины/Drw_System_Automation/УСТАНОВЩИК_Drew_AUTO.exe`, внутри — `install-flat.ps1`, блок удаления updater: папка `%LOCALAPPDATA%\CAD Booster\Drew` (старая копия updater) удалялась в любом режиме, а при установке для одного пользователя (`-User` или без прав) это и есть папка установки — Drew удалялся целиком. При обычной установке с правами администратора ловушка не срабатывала. | исправлено: папка пользователя чистится только при установке на весь ПК и если она не совпадает с папкой установки; остальные 84 файла установщика не изменены; установщик пересобран (SHA-256 C1B15DB9…), по новому отпечатку Drew один раз переставится на каждом ПК (З-56). В исходной папке сборки Drew нужен исправленный `install-flat.ps1`, иначе следующая сборка вернёт ловушку; T0 test_T0_drew_auto_removes_updater_but_keeps_drew — достаёт `install-flat.ps1` из exe и прогоняет блок в песочнице в обоих режимах |
| З-59 | 26.09.2026 | В окне настройки при запуске тикает таймер: на ПК, где установка уже есть, через 5 с обновление начинается само. Нужно, чтобы настройка запускалась только кнопкой. | `01_Настройки_SolidWorks/_Исходники/CAD_Workstation_Configurator.py`: отсчёт `AUTO_UPDATE_SECONDS` = 5 и `tick()` вызывали `start()` сами, первое «Закрыть» лишь отменяло отсчёт; README, руководство §1.2/§1.3, «Как работает система» и текст архива выпуска обещали «обновление начинается само». | исправлено: отсчёт убран, окно само ничего не запускает — только кнопка «Установить / Обновить»; при открытии — подсказка (`startup_status`): «Есть новый выпуск …», «В папке более ранний выпуск …», «Прошлая настройка завершилась с ошибками …» (LastResult = FAILED) или «Установлен текущий выпуск»; «Закрыть» закрывает сразу; тексты поправлены; окно пересобирается при публикации; T0 test_T0_configurator_starts_only_by_button — настоящее скрытое окно на подменённом реестре «установка есть»: всё, что окно поставило в очередь, выполняется, установка не начинается (start, run_engine, Popen, потоки подменены), кнопка запускает |
| З-60 | 26.09.2026 | Почистить копии установщика Drew во временной папке. | Шаг [7/9] копирует установщик в `%TEMP%\ESKD_Drew_<guid>` и убирает копию, только если окно установщика уже закрыто, а оно открыто до «Готово»; сам установщик Drew распаковывается в `%TEMP%\DrewGovSetup_<id>` и не удаляет её — ~50 МБ на каждую переустановку, на этом ПК за 26.09 скопилось 306 МБ (12 папок). Подсказка «закройте кнопкой «Закрыть»» называла не ту кнопку. | исправлено: 12 папок убраны из временной папки этого ПК (перенесены в папку «на удаление»); `Clear-EskdDrewTempLeftovers` (EskdDeploy.psm1) — в начале шага [7/9] (и при снятой галочке Drew) убирает хвосты прежних установок только по точному шаблону имени, только когда окно установщика Drew не открыто и папке больше часа (из свежей распаковки может ещё работать установщик Windows), папки со ссылками не трогает; подсказка — «Готово»; руководство §1.5; T0 test_T0_drew_temp_leftovers_cleaned, случаи в check_deploy_engine.ps1 (test_T0_deploy_from_readonly_toolkit_folder) |
| З-61 | 26.09.2026 | Drew разворачивается с моими настройками ГОСТ (шаблоны, форматки) или их каждый раз надо подтягивать? Решение владельца: эталон инструментария главный, у всех один; правку в окне Drew заменять не молча, а с предупреждением, и описать в руководстве. Форматы А2, А1, А0 в профиль Drew не добавлять — только А3 и А4 (решение владельца 26.09.2026). | Шаг [7/9], блок профиля оформления Drew: эталон `03_Макросы_и_Плагины/Drw_System_Automation/Drew-Blueprints.xml` (стиль «ГОСТ», пути через `%TOOLKIT%`) при каждой настройке с галочкой Drew записывался в `%APPDATA%\CAD Booster\Drew\Drew-Blueprints.xml` без сравнения — правка в окне Drew пропадала молча, копия в `%LOCALAPPDATA%\ESKD\Backups` делалась при каждой настройке, даже для одинакового файла. Разбор 26.09: профиль этого ПК побайтно равен эталону, все 16 копий — эталон; руководство про профиль молчало. | исправлено: `Update-EskdDrewProfile` (EskdDeploy.psm1) — равный эталону файл не трогается и не копируется; файл, который записала настройка (отпечаток `ESKD_Install\DrewProfileHash` или совпадение с нынешним или прежним эталоном — список `DrewProfileMasterKeys` — при любой папке инструментария и концах строк), заменяется молча; изменённый после настройки — заменяется эталоном с [ВНИМАНИЕ] и путём копии (ещё раз перед итогом); эталон не XML или файл занят — предупреждение, файл не тронут; `-Mode Uninstall` сохраняет отпечаток; руководство §1.5 п. 6 (как внести правку в эталон); T0 test_T0_drew_profile_toolkit_wins_with_warning, случаи в check_deploy_engine.ps1 |

## Подробности

### З-1. Формат чертежа детали не попадает в спецификацию

**Что видно.** Сборочный чертёж `NC3-7R.03.000 Перемычка` (формат А3): в таблице спецификации строка
`2  NC3-7R.03.002  Фланец  2` — графа «Формат» пуста. При этом у чертежа детали `NC3-7R.03.002 Фланец`
формат проставлен: в штампе внизу справа «Формат А4».

**Что проверить в коде:**
* кто заполняет графу «Формат» в спецификации — надстройка ЕСКД, SWPlus или SpecEditor;
* если формат берётся из свойства модели — записывается ли оно вообще (в `PropertyDictionary` графы «Формат» нет);
* если из чертежа — находит ли код чертёж детали рядом с моделью и читает ли размер листа
  (`DrawingDoc.GetSheetNames` → `Sheet.GetSize`, с поправкой на пользовательские форматки);
* нужно ли для БЧ-деталей ставить «БЧ» вместо формата (у `NC3-7R.03.001 Распорка L369` в графе стоит «БЧ»).

**Правку не начинать** до отмашки владельца.

### З-2. Тип отображения по умолчанию на чертеже

**Что нужно.** У всех рабочих мест новый вид чертежа должен создаваться в режиме «Невидимые линии
отображаются»: конструктор видит скрытые кромки и не строит лишних разрезов.

**Что известно.** В работающей сессии владельца настройка уже выставлена: API-параметр
`swHiddenEdgeDisplayDefault` (76) = 1. Реестровый профиль развёртывания несёт `[…\Drawings] "Display Mode"=dword:00000003`,
и это, судя по всему, другой параметр (или значение, ещё не переписанное SolidWorks — он сохраняет настройки при выходе).

**Исправление.** Ключ — `[…\Drawings] "Display Mode"`, значение совпадает с `swHiddenEdgeDisplayDefault`:
в резервной копии собственных настроек владельца (18.09.2026 10:38) стоит `1`, после установки с прежним профилем — `3`.
В профиле теперь `dword:00000001`; тест установки (`check_deploy_engine.ps1`) проверяет значение.
Тем же сравнением резервных копий подтверждён ключ библиотеки проектирования —
`ExtReferences\Content Manager Folders` (папка «_ крепеж и фурнитура» с NAS прописывается при установке).

### З-3. После очистки свойств повторное заполнение работает неверно

**Что видно.** Владелец очистил свойства детали или сборки и запустил заполнение заново. После этого
реквизиты не восстанавливаются правильно: не вносятся фамилии (Разработал, Проверил), обозначение
и наименование из имени файла, разбивка материала по полям (`Материал_Строка`, `Материал_Таблица`,
`Материал_ФБ`).

**Что проверить в коде:**
* как надстройка отличает «пусто, потому что очистили» от «пусто, потому что ещё не заполняли» —
  учёт происхождения значений (`Provenance`): не считает ли она очищенное значение ручным и потому
  не перезаписывает его;
* не остаются ли после очистки служебные следы (запись БЧ, отметка прежнего значения), из-за которых
  синхронизация решает, что всё уже заполнено;
* что именно очищалось и чем — вручную в окне свойств, макросом SWPlus (MProp/DProp) или кнопкой
  «Сброс»; от этого зависит, какие свойства пропали, а какие остались;
* воспроизвести на фикстуре: заполнить → очистить все свойства → «Синхронизировать» → сравнить
  с результатом первого заполнения.

**Правку не начинать** до отмашки владельца.

### З-4. MProp: «Несовпадение типов» при выборе базы материалов

**Что видно.** Окно MProp 2017 SP1.0 на детали `NC3-7R.04.002 Закладная царги`: ошибка VBA 80020005
«Несовпадение типов» в `CboBaseSW_Change`, строка разбора `classification name`.

**Причина.** MProp читает `.sldmat` целиком и делит по `vbCrLf`, ожидая одну классификацию или материал
на строку. После добавления полимеров библиотека была сохранена с LF: для MProp весь файл стал одной
строкой, и разбор имени группы ломался. SolidWorks при этом файл читал нормально — он разбирает XML.

**Исправление.** Все переводы строк — CRLF (заодно исправлены два старых одиночных LF), содержимое не
менялось, 128 материалов. Статический тест T0 теперь проверяет, что в файле нет LF без CR и что каждый
материал и классификация на отдельной строке. Выпуск 2026.09.18.1307 опубликован на NAS — SolidWorks
берёт библиотеку прямо оттуда.

### З-5. Неметаллические материалы не назначаются

**Что видно.** Деталь `NC3-7R.00.001 Сиденье пластиковое`: в окне «Материал» выбран полиэтилен из раздела 14,
«Применить» — в дереве по-прежнему «Материал <не указан>».

**Причина.** Не новые полимеры, а весь неметаллический раздел библиотеки: у ЛДСП, МДФ, фанеры, ДВП/ХДФ, кромки,
HPL и полимеров внешний вид ссылался на файлы `…\wood\oak\matte oak.p2m` и `…\plastic	exturedine textured.p2m`,
которых в SolidWorks 2025 нет (дерево теперь лежит в `organic\wood`). SolidWorks такой материал молча не назначает.
Проверка через API на пустой детали: металлы — да, все 45 неметаллов — нет.

**Исправление.** Блок внешнего вида заменён на стандартный из материалов SolidWorks: для древесных плит — «Дуб»
(`organic\wood\oak\polished oak 2d.p2m`), для кромки, HPL и полимеров — «ABS» (`plastic	extured\White-PW-MT11050.p2m`).
Физические свойства и реквизиты не менялись. Проверка через API на копии библиотеки: назначаются 128 из 128.
Статический тест T0 сверяет пути внешних видов с установленным SolidWorks и ищет управляющие символы в записях.

### З-6. Миниатюры моделей в окне операций ЛЗК

**Что нужно.** В окне «ЕСКД: Ведомость ЛЗК — операции» (кнопка «Ведомость ЛЗК») рядом с обозначением показывать
эскиз детали или сборки: по шифрам `NC3-7R.02.003` и т. п. трудно понять, какая это деталь и верно ли отмечены
операции (лазер листа / трубы, гибка, сварка, покраска).

**Как сделать (предложение):**
* первая колонка таблицы — миниатюра ~64×64, высота строки под неё;
* картинка — превью, которое SolidWorks хранит в самом файле модели: миниатюра проводника Windows
  (`IShellItemImageFactory`) или `ISldWorks.GetPreviewBitmapFile`; модели при этом не открываются, окно не тормозит;
* по наведению или выделению строки — увеличенный эскиз (~256 px) сбоку или во всплывающем окне;
* если превью в файле нет — пустая клетка, окно работает как раньше;
* проверка: модульный тест на построение строк без превью и e2e-прогон ЛЗК на фикстуре.

**Правку не начинать** до отмашки владельца.

### З-7. Площадь покраски сборки — только внешняя

**Как сейчас.** Если «Покраска» назначена сборке, её детали на лист «Покраска» не выводятся — двойного счёта
нет. Площадь сборки берётся как `SurfaceArea` документа сборки, а это сумма полных поверхностей всех тел:
* внутренняя поверхность профильных и круглых труб (у трубы 50×25×1,5 это около 45 % всей площади);
* грани, которыми детали прилегают друг к другу или свариваются (не окрашиваются);
* если в окрашиваемой сборке есть пластик или покупные — их поверхности тоже.

Цифры NC3-7R (м²): Опора 0,160 (Лежак 0,099 + Штанга 0,061), Укосина 0,144, Перемычка 0,062, Царга 0,104.
Для деталей-труб, окрашиваемых отдельно, внутренняя поверхность тоже входит.

**Предложение.** Окрашиваемая площадь = наружная поверхность металлических тел: тела деталей сборки
объединяются (грани касания и сварки уходят), пластик и покупные не берутся, внутренняя поверхность труб
не считается.

**Решение владельца (18.09.2026).** Считать только наружную поверхность окрашиваемых деталей; места сварки
и касания, пластик и покупные — пренебречь.

**Исправление.** `Core/PaintArea.cs`: полная площадь детали из SolidWorks умножается на долю наружного
периметра сечения по сортаменту «Материал_Строка» (прямоугольная, плоскоовальная, круглая труба); лист и
прочее — как есть. Окрашиваемая сборка — сумма наружных площадей её металлических деталей на одну сборку
со всеми вложенными узлами (`LzkService.PaintedAssemblyAreas`), пластик/древесные плиты и покупные не входят.
Модульные тесты `PaintAreaTests`.

### З-8. Обозначение исполнения в ведомости ЛЗК

**Что видно.** В «Ведомость_NC3-7R.xlsx» «Укосина» — две строки с одним обозначением `NC3-7R.02.000`.

**Причина.** У «Укосины» конфигурация «01» производная от «00». Синхронизация определяла исполнение по имени
корневой конфигурации («00» — базовое) и писала в «01» обозначение без суффикса. Главная сборка
(конфигурации 3–7 корневые) получила `-03`…`-07` правильно. Кроме того, надстройка собирала модели
ведомости по пути файла: конфигурации одного файла сливались в одну строку, обозначение бралось из
активной конфигурации модели.

**Исправление.** Исполнение — по имени самой конфигурации, корневая — только если своё имя не читается как
исполнение. Строка ведомости — «файл + конфигурация», свойства читаются из конфигурации, стоящей в изделии;
в окне операций галочки строк одного файла синхронны («Операции» у файла общие). Автотест M06b:
производная «03» от «00» → `…-03`, «Исполнение» = 2.

**После публикации:** пересохранить «Укосину» (синхронизация допишет `-01`) и сформировать ведомость заново.

### З-9. Деталь БЧ: запись в спецификации дублируется и наезжает

**Что видно.** Чертёж `NC3-7R.03.000 Перемычка`, спецификация, строка `БЧ 1 NC3-7R.03.001 Распорка`: дробь сортамента
«Труба ПО 30х15х1,5 ГОСТ 8644-68 / 08пс ГОСТ 13663-86» выведена дважды; «L = 369 мм» первой копии наложено на
знаменатель («L = 369мм ГОСТ 13663-86»), вторая копия — отдельными строками с массой 0,31 кг. Строка таблицы
разъехалась.

**Что известно из журнала надстройки:**
* запись БЧ живёт в свойстве «Наименование» (имя + перевод строки + дробь `<STACK>` + длина заготовки);
* при «Проверить изделие» (К-3, 16:55) и при каждом сохранении 17:10–17:11 надстройка переписывает «Наименование»
  заново («Распорка → Распорка⏎<STACK…>») — значит, сохранённое значение каждый раз считается устаревшим;
* в это же время файл переименован «Распорка L369» → «Распорка» и перенесён в `01_3D` (переименование меняет
  исходное наименование, из которого строится запись).

**Версии причины (проверить):**
1. Запись строится дважды: надстройка кладёт дробь в «Наименование», а SpecEditor/MProp ещё раз добавляет
   материал и длину для раздела «Детали» с форматом БЧ — две копии в одной ячейке.
2. Сравнение «сохранённое ≠ ожидаемое» не узнаёт уже готовую запись (другие переводы строк `

`/`
`,
   хвост после `</STACK>`), и каждое сохранение перестраивает её; при переименовании старая часть не удаляется.
3. SpecEditor неверно считает высоту строки для ячейки с `<STACK>` внутри многострочного текста — наложение «L = …».

**Дополнение 19.09.2026.** Владелец: спецификация меняется при формировании ЛЗК, если в изделии есть детали БЧ.
Это тот же путь: «Ведомость ЛЗК» записывает в модели «Операции» и «Габарит» и сохраняет их без вопросов
(`Sw/LzkService.cs`, `Save3`), а при каждом сохранении надстройка пересобирает запись БЧ в «Наименовании»
(`Sw/SyncService.cs` → `BchService.UpdateOnSave`). Отдельного теста «ЛЗК → спецификация сборки с БЧ» нет.

**Причина (доказана 19.09.2026 на копии «Перемычки»).** Графа «Наименование» таблицы SpecEditor связана со
свойством «Наименование» детали, и запись в её ячейку (`Text`, `Text2`) SolidWorks пишет в модель; разорвать связь
API не даёт. Раскладка записи БЧ по строкам (`SwpLayoutRecords`, WP-4.3) писала в связанную ячейку первую строку
записи — у «Распорки» на диске осталось «Наименование» = «Распорка». Следующее сохранение детали (ЛЗК, синхронизация,
«Проверить изделие») считало запись потерянной и восстанавливало её; связанная ячейка снова показывала все три строки
поверх строк, вставленных SpecEditor, — отсюда дубль и «L = …» на знаменателе. Версии 1–3 не подтвердились.
Попутно: в имя файла IGS (К-2) попадала вся запись («…Распорка__STACK size=1_…_L = 369 мм.igs»).

**Исправлено (решение владельца 19.09.2026 — вариант 1: всё читается из модели и пишется в свойства модели).**
* У детали БЧ «Наименование» — одно название, как у любой детали; строки после названия — свойство модели
  «Запись_БЧ» (`BchRecord.LinesProperty`). Кнопка «Деталь БЧ» и сохранение ведут оба свойства; запись прежних версий
  при первом сохранении делится без потерь; при снятии БЧ «Запись_БЧ» удаляется.
* SpecEditor (`09_Тесты/tools/swplus_z9_edits.py`): строки записи читает временной графой, связанной с «Запись_БЧ»
  (как графы «Раздел» и «Группа»), а в связанную ячейку «Наименования» пишет только то, чего в ней ещё нет, — у
  детали БЧ ничего. Свойства моделей SpecEditor больше не переписывает.
* Имя файла выдачи (PDF, DXF, IGS) берёт у записи БЧ только название (`ExportNaming.Stem`).

**Тесты:** S09 (оформление SpecEditor не меняет «Наименование», «Запись_БЧ», «Примечание» деталей; после сохранения
чертежа с деталями и повторного открытия таблица та же), B01–B09 под новое хранение, юнит-тесты
`Test_bch_record_splits_into_title_and_lines`, `Test_Bch_record_gives_only_its_title`.

**После публикации:** у изделий с деталями БЧ пересохранить детали (запись разделится) и заново запустить SpecEditor
на сборочном чертеже — таблица соберётся из «Наименования» и «Запись_БЧ».

### З-10. Спецификация на листе сборочного чертежа — в левом нижнем углу

**Что нужно.** Спецификация на листе сборки привязана к левому нижнему углу рамки: низ таблицы — на нижней линии
рамки, левый край — на 20 мм; таблица растёт вверх. На A3 и больших листах, как было изначально.

**Причина.** SpecEditor вставляет таблицу к точке привязки BOM форматки (`FrmSpecEditor:1708`,
`InsertBomTable4(True, …, BottomRight)`), правым нижним углом. Все форматки SWPlus и шаблон `Чертеж.drwdot` несли
точку листа A4 — (205; 68) мм, над основной надписью. На A3 таблица вставала посреди листа на высоте 68 мм, и её
двигали руками: у «Перемычки» таблица стоит в (205; 5) и уже отвязана от точки.

**Исправлено.**
* Форматки первого листа шириной от 395 мм (A3, A2, A1, A0 альбомные; A2, A1, A0 книжные) и шаблон `Чертеж.drwdot`:
  точка привязки BOM — (205; 5) мм. Спецификация шириной 185 мм встаёт левым нижним углом в (20; 5). Точка ставится
  штатно — точка эскиза форматки назначается точкой привязки (`ISheet.SetAsTableAnchor`), инструмент
  `09_Тесты/tools/set_bom_anchor.py`. Шаблон хранит свою копию форматки, и SolidWorks не перечитывает форматку с тем
  же именем, поэтому шаблон исправлен отдельно.
* A4 и A3 книжный не менялись: слева от основной надписи спецификация не помещается, она над основной надписью.
* SpecEditor (`09_Тесты/tools/swplus_spec_anchor_edits.py`, процедура `SwpBomAnchor`): у чертежа на прежней форматке
  перед вставкой спецификации один раз переставляет точку привязки его листа тем же способом.

**Тест:** S10 — форматки A3–A0 несут точку (205; 5); лист с прежней точкой SpecEditor исправляет; таблица привязана,
правый нижний угол в (205; 5), левый край на 20 мм; после раскладки записей БЧ низ остаётся на рамке. Проверено на
копии «Перемычки».

**После публикации:** запустить SpecEditor на сборочном чертеже — таблица встанет в угол сама. Уже стоящую
отвязанную таблицу SpecEditor в режиме новой спецификации удаляет и вставляет заново (`FrmSpecEditor:1590–1606`).


### З-11. Материал, назначенный в контексте сборки, не доходит до свойств

**Что видно.** Изделие «Печка для Сергея»: конструктор назначил материалы всем деталям правой кнопкой в дереве
сборки, выгрузил книгу ЛЗК — 19 замечаний «не заполнено „Материал"» из 24 строк. Материал стоял только у тех
деталей, которые он успел открыть и сохранить руками: между 21:11 и 21:13 их стало 1 → 4.

**Причина.** Синхронизация подписана на `FileSaveNotify` документа (`Sw/EventHub.cs:380`). Сохранение сборки
записывает изменённые детали на диск, но события по каждой детали при этом не возникает — `SyncMaterials`
для неё не отрабатывает, и `Материал_ФБ`, `Материал_Таблица`, `Материал_Строка` остаются пустыми. Книга ЛЗК
читает `Материал_Строка` и ставит «?». Кнопка «Синхронизировать» работала с активным документом: стоя в сборке,
она синхронизировала сборку, а не её детали.

**Исправлено.** `Sw/BatchSyncService.cs`: «Синхронизировать», нажатая в сборке, обходит изделие целиком.
Каждой детали — то же, что при сохранении, плюс подбор материала по геометрии, затем сохранение. Проход двойной:
сначала все детали осматриваются, и только потом один раз спрашиваются неоднозначные типоразмеры — лист 6 мм
в десяти деталях даёт один вопрос, а не десять окон.

Обход идёт по всему дереву, а не по верхнему уровню: `GetComponents(false)` отдаёт и детали подсборок второго,
третьего и любого следующего уровня. Каждый файл берётся один раз, как бы часто он в изделии ни встречался.

Границы обхода: только детали из папки изделия. Покупные, крепёж из библиотеки и детали чужих заказов лежат вне
её и не трогаются; файлы «только для чтения» пропускаются с замечанием. Стандартные и покупные изделия, лежащие
внутри папки заказа, защищены разделом и реквизитами поставщика (`SyncService.IsProtected`). На время обхода
обычная синхронизация по событию сохранения отключается (`BatchSyncService.Running`) — свойства уже записаны
этим же проходом.

Сохраняются только те детали, в которых обход что-то изменил. Изделие целиком — это и давно согласованные
детали: переписывать их файлы незачем, у них меняется только дата, и потом не понять, что в заказе правили.

**Тесты:** T07 — детали заводятся без материала при выключенном подборе (так они и приходят от конструктора),
затем обход изделия заполняет обе; T08 — деталь вне папки изделия остаётся нетронутой; T09 — готовая деталь
обходом не переписывается, дата файла та же; T10 — деталь на третьем уровне дерева (сборка в сборке в сборке)
обходом из корневой сборки заполняется.

**Проверено на настоящем изделии.** Копия каталога `01_3D` заказа NC3-7R (оригиналы не открывались), 17 файлов
деталей, подсборки второго уровня. У двух деталей внутри подсборок материал снят при выключенной службе
надстройки; одно нажатие в корневой сборке: «деталей 15, обновлено 5, сохранено 5, материалов назначено 2»,
`Материал_Строка` у обеих заполнена, переписано 5 файлов из 17 — стандартные изделия и пластик не тронуты.

### З-12. Материал не сверялся с геометрией детали

**Что видно.** Два случая, оба боевые.

Первый: «Передняя стенка» изделия «Печка для Сергея» — обозначение `560×605×8`, заготовка `605×560×8`,
а материал «Лист Б-ПН-НО-**6,0**». Толщина в модели одна, в сортаменте другая; такое уедет в закуп.

Второй: сварная деталь из плоскоовальной трубы 30×15×1,5, материал «не указан». В библиотеке такая труба одна —
вводить материал руками незачем, он должен подтянуться сам.

**Причина.** Надстройка не смотрела на геометрию вообще. `SyncMaterials` (`Sw/SyncService.cs`) спрашивала у
SolidWorks назначенный материал и переносила его в свойства; свойство `Типоразмер` библиотеки (есть у всех 128
материалов) `MaterialCatalog.Parse` не читал. Толщина листа читалась в двух местах ради имён DXF и проверки
изделия, но с материалом не сверялась.

**Исправлено.** Подбор материала по геометрии:

* `Core/StockCatalog.cs` — чистая логика: нормализация типоразмера («40Х20Х1,50» = «40x20x1.5»), нормализация
  ГОСТа, подбор по паре «ГОСТ сортамента + типоразмер». Записи, дающие одинаковые свойства, считаются одной:
  лист 8 мм лежит в библиотеке в двух редакциях ГОСТ 14637, но в свойствах от них одно и то же — значит выбирать
  не из чего. Лист 6 мм даёт два варианта, Ст3сп и 09Г2С, — это настоящий выбор. Из схлопнутых записей
  назначается та, чьё имя не спорит с её же свойствами: «Лист 3,0 … / Ст3сп ГОСТ **14637-89**» имеет свойство
  `ГОСТ_Материал = ГОСТ 16523-97` — имя осталось от прежней редакции, и в дерево детали ему попадать незачем.
  При равенстве берётся запись со свежей редакцией ГОСТа в имени.
* `Sw/StockService.cs` — чтение геометрии без отката модели: типоразмер и ГОСТ из папки списка вырезов (свойства
  профиля доходят туда сами), толщина из `ISheetMetalFeatureData.Thickness`. Если список вырезов ещё не построен,
  профиль читается из самого элемента конструкции (`WeldmentProfilePath`).
* `Sw/StockPickForm.cs` — окно выбора, одна строка на типоразмер.
* Назначение — только из очереди простоя: в обработчике сохранения SolidWorks ещё держит документ.

**По геометрии не гадать** (решение владельца 21.09.2026). Толщина берётся только из элемента листового
металла, типоразмер — только из списка вырезов или файла профиля. Плоское тело, построенное обычной
вытяжкой, не проверяется: это может быть пластик, фанера, ЛДСП — габарит о прокате не говорит ничего.
Закреплено живым тестом T11.

**Правило владельца** (решение 20.09.2026): материала нет и подходящий один — подставить молча; подходящих
несколько — спросить; подходящих нет — замечание; материал выбран и совпадает — ничего; материал выбран и не
совпадает — уведомление, значение не менять. Последнее принципиально: ошибка может быть и в модели, решать
конструктору.

Этой сверкой ловится и ошибка в отгруженном изделии: у `NC3-7R.02.001 Стойка` стоял материал прямоугольной трубы
ГОСТ 8645-68, а профиль плоскоовальный ГОСТ 8644-68. Типоразмер `30х15х1,5` есть в обоих сортаментах профилей,
поэтому подбор без ГОСТа запрещён — иначе надстройка повторила бы ту же ошибку.

**Тесты:** 18 юнит-тестов на настоящей библиотеке (`09_Тесты/unit/StockTests.cs`) и живые T01–T08
(`09_Тесты/tests/test_e2e_stock.py`) — по сценарию на каждую строку правила и на обход изделия.
Сверх того — прогон на копиях девяти деталей заказа NC3-7R (оригиналы не открывались): все девять дают
вердикт «Ok» с верной записью библиотеки, включая листовые детали, подобранные по толщине 3 мм.

### З-13. Длина заготовки бралась по габаритному ящику, а не из списка вырезов

**Что видно.** Вопрос владельца: деталь строится по эскизу, а потом её удлиняют бобышкой или отрезают
плоскостью; идти она может и по диагонали, между осями. Какая длина уходит в ведомость и в «Расход»?

**Причина.** Книга ЛЗК читает длину из свойства папки списка вырезов — и правильно делает: SolidWorks считает
её сам вдоль самой детали, а не по осям, и после отреза пересчитывает. Но читала она свойство по имени
`LENGTH`, а русский SolidWorks называет его «ДЛИНА». Свойство не находилось никогда, и книга уходила в
запасной путь: наибольший размер габаритного ящика по осям детали, строка помечалась звёздочкой «оценка»,
в «Расходе» стояло «L по габариту — уточните длину заготовки». Для детали вдоль оси число случайно совпадало
с настоящей длиной, для наклонной — нет.

**Исправлено.** `Core/CutListProperties.cs`: нужное свойство ищется не по имени, а по ссылке внутри записанного
значения — SolidWorks пишет туда «LENGTH@@@&lt;папка&gt;@&lt;файл&gt;», и левая часть английская при любом языке
интерфейса. Имя остаётся запасным признаком, если свойство вписали руками. Так же ищется «КОЛИЧЕСТВО».
«ОБЩАЯ ДЛИНА» за длину заготовки не принимается: это длина всех заготовок папки вместе.

**Проверено на настоящем изделии.** Копия подсборки `NC3-7R.02.000 Укосина`, книга ЛЗК построена надстройкой:

| Деталь | Было | Стало |
|---|---|---|
| `NC3-7R.02.001 Стойка` | `556×40×20*` (оценка) | `L=556` |
| `NC3-7R.02.002 Кронштейн` | `125×30×15*` | `125×30×15*` — у папки списка вырезов нет свойства длины вовсе |

Вторая строка — случай, когда SolidWorks длину не считает вовсе: тело элемента конструкции дорабатывали
(отрезали, приварили), и свойства длины в папке нет. Обновление списка вырезов её не возвращает — проверено.

**Исправлено, часть 2** (решение владельца 21.09.2026): если длины в списке вырезов нет, книга меряет тело
сама — вдоль его самого длинного прямого ребра (у прямого проката оно идёт по оси, и при косых резах тоже),
протяжённость по крайним точкам тела (`Core/BodyLength.cs`, `LzkService.MeasuredLength`). Путь элемента
конструкции для оси не читается: он требует `AccessSelections`, а это откат модели. Из измеренного и наибольшего
габарита по осям берётся большее — страховка владельца на случай неверной оси: в закуп уйдёт длинный хлыст,
а не короткий. Габарит по осям у наклонной детали занижает: труба 500 мм под 45° даёт по осям 354.
Рёбра сравниваются по хордам, и только победитель спрашивается, прямой ли он, — вопрос о форме это отдельный
вызов SolidWorks на ребро. Цена: около 0,2 с на деталь снаружи процесса (внутри надстройки дешевле), и только
там, где длины нет; открытие документа стоит 2 с. Такая длина помечена «*» и пометкой «измерена по модели».
На той же укосине «Кронштейн» стал `L=125*`.

**Тесты:** 6 юнит-тестов (`09_Тесты/unit/CutListTests.cs`) на значениях из настоящей детали заказа;
5 юнит-тестов арифметики измерения (`09_Тесты/unit/BodyLengthTests.cs`); живой L09 — труба 500 мм под 53°,
по осям 424, в книге `L=500*`.
