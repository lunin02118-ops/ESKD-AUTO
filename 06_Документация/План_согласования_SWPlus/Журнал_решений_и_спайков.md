# Журнал решений и спайков — план согласования с SWPlus (ред. 4)

Ветка `refactor/v6-eskd`. Записи идут по мере выполнения плана; номера пакетов и находок — по
`План_согласования_ЕСКД_SWPlus_ред4.html`.

## 13.09.2026 — решения владельца перед реализацией

| № | Решение |
|---|---|
| Р-1…Р-13 | приняты (план, раздел 5) |
| Библиотека, вопрос 1 | знаменатель проката коротко: «марка ГОСТ» |
| Библиотека, вопросы 2–12 | как есть |
| О-7 (а…е) | «как советуешь»: (а) кромка без документа; (б) лист Ст3сп 2,5 мм — ГОСТ 16523-97; (в) у ЛДСП убрать знаменатель ГОСТ 10632-2014; (г) 14918-2020 и (д) 22233-2018 — только год; (е) рифлёный лист, труба ВГП, АМг2.М, полиэтилен, полипропилен — одной строкой |
| Старт | «делай» — реализация по шагам 0–6 |

## Шаг 0 — выполненные пакеты

| Пакет | Коммит | Итог |
|---|---|---|
| WP-0.1 пакет фикстур | `802516d` | фикстуры корпуса А с материалом из библиотеки, корпус Б, отметки о происхождении (A-14, AD-25) |
| WP-0.2 выгрузка VBA | `acb2c86` | `03_…/_VBA_выгрузка`, `09_Тесты/tools/export_vba.py`, T0 «выгрузка = .swp»; SHA-256 пяти `.swp` совпали с приложением А |
| WP-0.3 golden master | `0a8a7f1` | правило без срабатываний проваливает R01; AD-17/21/22 ограничивают `old`. Проверка на снимке R01 прогона `20260913_143501`: 6920 различий, необъяснённых 0, неиспользуемых правил 0 (AD-05 — `optional`) |
| WP-6 корпус ред. 4 | `c94a701` | A-15 кандидат БЧ (труба 40×40×2, L 500), A-16 «ЭМ-Детали», A-17 легче 100 г (18,8 г), A-18 исполнения 50,2 г / 175,8 г, A-19 длинное наименование (59 знаков), A-20 исполнение в имени файла |

## Спайки S-1…S-8

Скрипт `09_Тесты/tools/spikes_step0.py`, прогоны `08_Результаты_Тестирования/runs/spikes_step0_20260913_223550`
(S-6, S-8) и `spikes_step0_20260913_223634` (S-2). Надстройка не загружалась, все документы — копии.

| Спайк | Результат | Следствие |
|---|---|---|
| S-1 «применить без правок» MProp из тестов | **Подтверждён 13.09**: `RunMacro2(<копия MProp.swp>, "MProp_run", "main_reload", 1, err)` применяет форму без окон за ≈ 1 с; MProp запускается из копии папок SWPlus в каталоге прогона (пишет `MProp_Prof.txt`, `MProp_Project.txt` рядом с собой) | эмулятор MProp на шаге 2 не нужен; К-1 и К-2 — живым вызовом |
| S-2 дробь и три строки в таблице BOM | **Рисует.** Шаблон `SpecEditor_sp.sldbomtbt`, запись «Пластина опорная⏎`<STACK size=1>`Лист Б-ПН-НО 4 ГОСТ 19903-2015`<OVER>`Ст3сп ГОСТ 14637-2024`</STACK>`⏎L = 200 мм» в «Наименовании»: в PDF три строки, дробь с чертой; «Формат» = «БЧ», «Примечание» = «0.63 кг» в своих графах | Р-4 без изменений: запись БЧ в «Наименовании»; О-2 закрыт |
| S-3 графа 5 в формате MProp | Выполнен аудитом штампа 13.09 (прогон `stamp_matrix_20260913_195225`): на текущих форматках масса MProp прижата к нижней линии (−1,8…−0,6 мм от центра), у надстройки по центру | правится положение `MYPRP15` в форматках (WP-4.6), MProp не трогается |
| S-4 перенос и шрифт графы 1 | Выполнен спайком варианта А (`layout_a_spike_20260913_202705`): при 5 мм шрифтом GOST type A в строку помещается ≈ 22 знака, при 3,5 мм ≈ 31; разметка MProp: 1 строка `<FONT size=4> ⏎<FONT size=5>`, 2 строки `<FONT size=2> ⏎⏎<FONT size=5>`, 3 строки `<FONT size=3.5>` | правило переноса WP-1.1: до 22 знаков — одна строка 5 мм, до 44 — две строки 5 мм, дальше — три строки 3,5 мм по 31 знаку |
| S-5 какой код SpecEditor исполняется | **Статически:** исходник `FrmSpecEditor:1006, 1697` — `CustomPropertyView <> "ОТСУТСТВУЕТ" Or … = "Default"`; скомпилированный код (P-code) — прежнее условие `<> "По умолчанию" Or … = "Default"`. Какой из двух исполняет SolidWorks 2025, без запуска формы не проверить: VBA перекомпилирует исходник, только если P-code собран другой версией VBA | вопрос снимается перекомпиляцией WP-4.3 (исходник и P-code станут одинаковыми); ошибка 91 проверяется ручным чек-листом шага 6. По S-8 у листа с пустым `CustomPropertyView` условие P-code истинно, вид с пустым именем не находится — вероятная причина ошибки 91 |
| S-6 свойства форматки в чертеже | **Переносит.** Новый чертёж из шаблона и `SetupSheet5` листа 1: только `SWFormatSize`. `NewSheet3` с форматкой A4-P-2 добавляет в чертёж 14 свойств форматки: `Разработал`, `DrawnBy`, `DrawnDate`, `Разраб.`, `Конструктор`, `Автор`, `п_Разраб`, `п_Разраб_Дата`, `Контора`, `Организация`, `Организация_ФБ`, `Компания`, `Firm`, `Organization`; замена форматки их не удаляет | WP-4.2: личные данные и алиасы v5 удаляются из форматок и шаблонов Master; чертежи, где они уже есть, чистит «Очистка v5» |
| S-7 пишет ли Drew свойства моделей | **Статически — нет.** В `CADBooster.Drew.Drawing.dll` и `CADBooster.Common.Core.dll` есть только чтение (`CustomPropertyExists`, `GetCustomProperty`, имена листов и колонок таблиц из свойств); вызовы записи (`SetCustomProperty`, `DeleteCustomProperty`, `Add3`, `Delete2`) есть лишь в библиотеке `CADBooster.SolidDna.dll`, и Drew на них не ссылается. Динамический вызов через отражение не исключён | Drew в таблицу записи 4.1 не добавляется; В-6 закрыт |
| S-8 `CustomPropertyView` листа | **Воспроизведён.** Чертёж владельца «Болт М6-6gх20.58 ГОСТ 7798-70» (форматка по пути `\\Home-pc\…`, три вида): `""` (пусто). A-10 лист 1 и A-11 — «По умолчанию», A-10 лист 2 (добавлен `NewSheet3` с пустым видом) — `""`; новый чертёж из шаблона — «По умолчанию» | WP-3.5: MProp и DProp принимают «По умолчанию», «Default» **и пустое значение** (и любое имя, которого нет среди видов листа) как «первый вид листа», без окна. Шаблон чертежа менять не нужно |

## S-2b — запись БЧ в спецификации по ГОСТ (замечание владельца 13.09: «отступы не по ГОСТ»)

Снимок S-2 был «сырой» таблицей BOM без оформления SpecEditor: текст по центру, записи в графах по середине высоты.
Проверено по первоисточникам и повторным спайком с оформлением строк, как делает SpecEditor
(прогон `spikes_step0_20260913_224514`, снимки `спайки/S2b_*.png`).

**Нормы.**
- ГОСТ Р 2.106-2019 п. 4.2.17.5: для деталей без чертежей в графе «Наименование» — наименование, материал и другие
  данные для изготовления; в графе «Формат» — «БЧ»; масса — в «Примечании».
- ГОСТ Р 2.105-2019, раздел 7 (документы с текстом, разбитым на графы): 7.2 — заголовки разделов с прописной буквы и
  подчёркнуты, одна свободная строка ниже, не менее одной выше; 7.4 — записи проводят на каждой строке в один ряд;
  7.5 — если в графе текст в несколько строк, в последующих графах записи начинают **на уровне первой строки**
  (однострочную запись машинным способом допускается ставить на уровне последней). Высота строк таблицы — не менее
  8 мм (6.8.5).
- Проект ГОСТ Р 2.109 (первая редакция, `06_Документация/v.1_ПР_ГОСТ Р 2.109…pdf`), п. 6.4: вид сортамента, затем
  обозначение сортамента и материал через горизонтальную или наклонную черту; **рисунок 15** — спецификация с
  деталями БЧ: «Полка» / «Уголок `20×20×3 ГОСТ 8509-93` над `Ст 3 ГОСТ 535-2005`» / «L=24±0,5мм»; каждая строка
  записи — отдельная строка спецификации по 8 мм, дробь занимает две строки, **вид сортамента («Уголок», «Круг») стоит
  слева от дроби**, весь текст — от левого края графы; «БЧ», позиция и обозначение — на первой строке записи.

**Что делает SpecEditor** (FrmSpecEditor:2721–2743, 3026–3076): «Обозначение», «Наименование», «Примечание» — по левому
краю, остальные графы по центру, всё — по верху ячейки; многострочная запись получает шаг строк 8 мм и высоту k × 8 мм.
Заголовки разделов — по центру и подчёркнуты. Это соответствует 7.2 и 7.5.

**Результат спайка.**
| Вариант | Вид |
|---|---|
| V1 запись надстройки v6: «Лист» внутри числителя, «100х200 мм» | вид сортамента в числителе — не как рис. 15 |
| V2 как рис. 15: «Лист `<STACK>`Б-ПН-НО 4 ГОСТ 19903-2015`<OVER>`Ст3сп ГОСТ 14637-2024`</STACK>`», размеры отдельной строкой | совпадает с рис. 15: слово «Лист» слева от дроби, дробь на двух строках, текст от левого края; запись занимает 40 мм (5 строк по 8 мм — одна строка под записью пустая) |
| V3 пустая строка после дроби | лишняя пустая строка — отклонено |

**Найдено попутно:** шрифт GOST type A в SolidWorks рисует знак «×» (U+00D7) как «Ч» («200Ч100»). В записях свойств,
выводимых в штамп и спецификацию, размеры разделяются буквой «х», как сейчас в библиотеке; замена на «×» запрещена
(совпадает с ответом «как есть» на вопрос 12).

**Следствие для плана (WP-2.7, WP-3.2, S03, B04).** Запись БЧ в «Наименовании» — форма рисунка 15:
1. строка 1 — наименование детали;
2. строки 2–3 — вид сортамента, пробел, дробь `<STACK size=1>`сортамент без вида`<OVER>`марка`</STACK>`
   (как в графе 3 MProp);
3. строка 4 — размеры: у проката — «L=<длина> мм», у листа — «<длина>х<ширина>» (буква «х»).
Оформление строк делает SpecEditor; тест S03 сравнивает изображение спецификации после SpecEditor с эталоном,
утверждённым владельцем; условие «5 строк вместо 4» — на утверждение владельцу.

## 13.09.2026 — решение владельца по виду записи БЧ; S-2c, S-2d и шрифт

**Решение владельца.** Вид записи — вариант V1 (как пишет надстройка: «Лист» в числителе, размеры «100х200 мм»), но
без обрезки текста и плотнее — без пустой строки под записью.

**S-2c** (`spikes_step0_20260913_225312`): в V1 правый край «19903-2015» в 0,16 мм от линии графы — текст упирается
в границу (обрезка); сужение шрифта графы до 0,9 даёт зазор 6,4 мм.
**S-2d** (`spikes_step0_20260913_225431`): минимальная высота записи при межстрочном интервале графы
4,4 мм (как ставит SpecEditor) — 34,8 мм → 5 строк; 3,9 мм — 32,8 мм → 5 строк; **3,4 мм — 30,8 мм → 4 строки**
(снимок `спайки/S2d_V1_плотно_4_строки.png`); 2,9 мм — 28,8 мм → 4 строки. Выбрано 3,4 мм.

**Следствие для плана.** Оформление строк таблицы делает SpecEditor (надстройка на него не влияет), поэтому нужна правка
SpecEditor в пакете WP-4.3 (FrmSpecEditor:3026–3076): для записи с дробью `<STACK>` — межстрочный интервал 3,4 мм и
число строк по минимальной высоте; если самая длинная строка графы шире графы без 2 мм — сжатие ширины шрифта, но не
меньше 0,8. Тесты S03, B04: зазор справа не меньше 1 мм, запись — целое число строк по 8 мм без пустой строки.

**Шрифт и знак «×»** (`font_x_spike_20260913_225141`, скрипт `09_Тесты/tools/spike_font_multiply.py`):
- «GOST type A» (`GOST_A.TTF`, ASCON) — в таблице кода U+00D7 стоит глиф буквы «Ч»; настоящего «×» и длинного тире
  нет. Этим шрифтом оформлены форматки, SpecEditor (`MyProperties_2.ini`, `SpecEditor.ini`) и Master (`Master.ini`).
  В `05_Шрифты` его нет — установщик не ставит его на ПК конструкторов (на этой станции он пришёл с КОМПАС-3D).
- «ГОСТ тип А» (`05_Шрифты`) — тоже «Ч» вместо «×».
- «GOST Type AU» (ASCON, Юникод) — «×» настоящий; ширины букв как у GOST type A, в репозитории нет, распространяется с КОМПАС-3D.
- **«GOST 2.304 type A»** (`05_Шрифты/GOST2304A_1.ttf`, ставится установщиком) — «×» и «—» настоящие, PDF из SolidWorks
  верный; высота строки как у GOST type A; шире буквы «А» (+2 %), «а» (+15 %), точка, запятая, пробел (−6 %), дефис (−12 %).
Предложение на решение владельцу: перевести форматки, шаблон чертежа, формы и таблицы SpecEditor, шаблоны Master на
«GOST 2.304 type A» в пакете WP-4.6 (координаты и границы переноса всё равно подбирает автотест штампа К-12); в свойствах
писать размеры знаком «×».

**Решения владельца 13.09.2026 (вечер):**
- Р-14 **Шрифт — «GOST 2.304 type A»** для форматок, шаблона чертежа, форм и таблиц SpecEditor, шаблонов Master и
  `.ini` SWPlus (пакет WP-4.6, координаты подбирает автотест К-12); размеры в свойствах — знак «×».
- Р-15 **Правка SpecEditor разрешена**: плотная запись БЧ — интервал 3,4 мм у записи с дробью, число строк по минимальной
  высоте, сжатие ширины шрифта при выходе за графу (пакет WP-4.3). Вид записи — V1.

## 13.09.2026 — «в графу «Обозначение» спецификации попадает наименование» (замечание владельца)

**Причина** (проверено `designation_probe_*`): в шаблонах `Деталь.prtdot` и `Сборка.asmdot` общее «Обозначение» =
`$PRP:"SW-File Name"`. Это выражение даёт имя файла целиком — «ПРТИ.468211.101 Пластина опорная». Пока документ не
сохранён с надстройкой и не прошёл MProp, таблица спецификации и штамп показывают его в графе «Обозначение». В спайках
S-2 надстройка не загружалась — отсюда снимок. После сохранения с надстройкой — «ПРТИ.468211.101» (общие и «00»).
Ошибка проявилась бы и на ПК с выключенной или неустановленной надстройкой.

MProp сам пишет `$PRP:"SW-File Name"` только при выключенном разборе имени (`prpFileName = 0`) или если в имени нет
пробела (FrmMProp:2517–2525) — тогда выражение равно обозначению, это верно.

**Исправление:** в шаблонах детали и сборки «Обозначение» пустое (`normalize_templates.py --apply`); пустое значение
надстройка и MProp заполняют по имени файла так же, как выражение. I03 проверяет, что в шаблонах нет
`SW-File Name` в «Обозначении». Прогон `20260913_225937`: I03, I06, P01, P02 зелёные.

## 13.09.2026 — живой сценарий «Сохранить → БЧ → спецификация → MProp» на текущей надстройке (до шагов 2–4)

Скрипт `09_Тесты/tools/live_bch_scenario.py`, прогон `live_bch_20260913_230513`; тесты B01–B06, P01–P05, M02, M06 в
прогоне `20260913_*` зелёные (они проверяют значения свойств, а не вид спецификации и не MProp).

| Шаг | Результат |
|---|---|
| Новая деталь из шаблона → «Сохранить как» «ПРТИ.468211.120 Пластина опорная» | верно: «Обозначение» ПРТИ.468211.120 (общие и «00»), «Наименование» Пластина опорная, дробь материала, масса |
| Сборка «ПРТИ.468211.121 СБ Узел проверочный» | обозначение и наименование верны; «Сборка1_ФБ» = « СБ» с пробелом и «Сборка2_ФБ» = «Сборочный чертёж» — формат надстройки v6, MProp перепишет (Н-04, Р-3, шаг 3) |
| «Деталь БЧ» → «Сохранить» | «Формат» БЧ, запись в «Наименовании», «Примечание» «0,63 кг» (запятая — Р-1/Р-4, шаг 3) |
| Спецификация (оформление как SpecEditor) | запись есть, но длинный числитель «Лист Б-ПН-НО-4,0 ГОСТ 19903-2015» переносится внутри дроби, знаменатель налезает на строку размеров (`спайки/live_1_…png`) — Р-15 (WP-4.3) и сокращение знаменателя по WP-4.5 |
| MProp «применить без правок» → «Сохранить» | **запись БЧ стёрта**: «Наименование» = «Пластина опорная», в спецификации нет материала и размеров (`спайки/live_2_…png`); «Примечание» стало живой массой MProp «… кг»; добавлены «RenameSWP», «Раздел», «Сборка1_ФБ», «Сборка2_ФБ»; «Масса_ФБ» и «Наименование_ФБ» — в разметке MProp. Надстройка при следующем сохранении запись не восстанавливает. То же сделает DProp «Перезагрузка форматки» (запускает MProp). Исправление — WP-3.2 (правка MProp) и WP-2.7 (шаг 3) |

**Временное правило до шагов 3–4:** у детали БЧ не запускать MProp и «Перезагрузку форматки» DProp; если запустили —
снова нажать «Деталь БЧ» дважды (снять и поставить) и сохранить.
