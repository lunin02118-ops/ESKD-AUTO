# План реализации и отладки по замечаниям владельца (ТЗ-02б)

**Дата:** 17.09.2026. **Основание:** [ТЗ-02б](ТЗ-02б_Замечания_владельца_2026-09-17.md), ТЗ-02 (Т-10, Т-14а…Т-14в, Т-35…Т-37), регламент КТО v3.1.
**Принцип отладки на каждом этапе:** правка → unit-тест → static → e2e на фикстуре → **боевой прогон на копии реального заказа** (`NC3-7R`, `CN1-2`) → отчёт владельцу. Копии заказов лежат в `D:\Work\_dev\_lzk_боевой\`, оригиналы не трогаются.

---

## Этап 1. Ведомость ЛЗК до рабочего вида (замечания З-1…З-4)

### 1.1. Шрифты книги (З-2)

Шаблон SWTools собран из китайского `bom_шаблон.xlsx`: базовый шрифт книги — `宋体` (SimSun), он наследуется всеми листами, включая созданные надстройкой.

- `Core/Xlsx.cs`: метод `NormalizeFonts(string faceName)` — в `xl/styles.xml` у каждого `<font>` заменяются `name` и `charset` (204 — кириллица), семейство 2; вызывается при записи ведомости.
- Шрифт — **Arial**: он есть на каждом ПК, читается в таблице лучше Times New Roman и совпадает с тем, что просит цех.
- Тот же шрифт закладывается в сам шаблон в репозитории SWTools (`scripts/build_lzk_template.py`), чтобы новые выгрузки приходили уже правильными; в ЕСКД остаётся нормализация — она защищает от чужого шаблона.
- **Тест:** unit `Test_Workbook_has_no_cjk_fonts` — в готовой книге нет ни одного `<font>` с иероглифическим именем и у всех проставлен charset 204.

### 1.2. Вёрстка листов «Покраска» и «Покупные» (З-3)

Сейчас заголовок листа стоит в колонке `A` шириной 5 знаков со стилем «перенос по словам» — отсюда текст в столбик.

- `Core/Xlsx.cs` дополняется: `Merge(range)`, `SetRowHeight(row, height)`, `FreezePanes(cell)`, `SetPrintSetup(area, repeatRow, fitToWidth)`, расширенный `AddStyle(bold, border, wrap, align, size, fill)`.
- Раскладка листа (одинаковая у обоих):
  - строка 1 — название листа, объединено по ширине таблицы, Arial 12 жирный, высота 22, без переноса;
  - строка 2 — изделие и дата (серый, 9);
  - строка 3 — шапка таблицы: жирная, по центру, перенос, высота 30, тонкие рамки, серая заливка;
  - строки данных — рамки, текст слева, числа справа (числовой формат `0.000` для площадей, `0` для количества);
  - итоговая строка — жирная с рамкой;
  - закрепление областей под шапкой, повтор шапки при печати, область печати, A4 книжная, вписать в ширину одной страницы.
- **Тесты:** unit `Test_Paint_sheet_layout` и `Test_Purchased_sheet_layout` — проверяются ширины колонок, объединение заголовка, стиль шапки, наличие `printOptions`/`pageSetup`/`freeze`, отсутствие переноса у заголовка.

### 1.3. Покраска по сортаменту (З-1)

Правило (по словам владельца и Т-14б):

| Что | Красится | Обоснование |
|---|:-:|---|
| Деталь из проката: лист, труба, профиль, уголок, швеллер, круг, квадрат, полоса, лента | **да** | металл под покраску |
| Сварной узел (сборка со швами или свойством сварной конструкции) | **да**, целиком | красят узел, его детали на лист «Покраска» не выводятся (Т-14б) |
| Деталь из пластика, ЛДСП, МДФ, ДВП, ХДФ, фанеры, кромки | нет | не красятся |
| Покупное и стандартное изделие | нет | приходит готовым |
| Механическая сборка | нет | красятся её составные части по своим правилам |

- `Core/Lzk.cs`: `LzkMaterials.IsRolledMetal(material)` — распознавание по первому слову сортамента и марке материала, списки проката и неметаллов — константами в одном месте, чтобы правка была одной строкой; неизвестный материал → не красится и попадает в замечания отчёта («материал не распознан, покраску проверьте вручную»).
- `LzkOperations.Suggest(ModelTraits)` получает материал и добавляет `Покраска` после резки/гибки для проката и для сварной сборки.
- Конструктор по-прежнему может снять флажок в окне «Операции» — автоподсказка помечается «(авто)» (Т-14б, Р0-5).
- **Тесты:** unit `Test_Suggest_paints_only_rolled_metal` (лист/труба/профиль → покраска; ЛДСП/фанера/пластик/ABS → нет; сварная сборка → покраска; механическая → нет), `Test_Unknown_material_is_not_painted_and_warns`.

### 1.4. Код покупных (З-4)

По Т-14а код 1С берётся из листа «Коды 1С» справочника; справочника пока нет.

- Пустой код остаётся **пустым**, `?` не пишется, замечание в отчёт не добавляется (это не ошибка конструктора).
- Единица измерения разводится на две колонки: «Ед. изм.» (`шт`) и «ОКЕИ» (`796`), значение `796 ШТ` из модели разбирается.
- **Тест:** unit `Test_Purchased_without_code_leaves_cell_empty`.

### 1.5. Приёмка этапа 1

1. `python run_tests.py unit` и `static` — зелёные.
2. `python run_tests.py e2e -k "L01,L02,L03"` — зелёные; L01 дополнительно проверяет: лист «Покраска» содержит стойки и пластину (прокат) и не содержит пластиковых деталей и покупных; шрифт книги — Arial.
3. Боевой прогон на копиях `NC3-7R` и `CN1-2`: ведомость открывается в Excel без иероглифов, шапка на месте, покраска заполнена, покупные без `?`.
4. Файлы ведомостей отправляются владельцу на проверку.

---

## Этап 2. К-3 «Проверить изделие» (ТЗ-02 Ш5, Т-32…Т-34)

`Core/ProductLocator.cs` (поиск изделия и заказа, Т-7а), `Core/CheckRules.cs` (перечень и уровни), `Sw/CheckService.cs`: обход ссылок главной сборки, `ForceRebuild3`, `DiagnoseActiveDocument`, чертёж или признак БЧ, заполненность `Операции`, файлы выдачи, ведомость и её контрольная сумма, оборванные размеры. Отчёт `_Проверка.txt` (UTF-8 BOM), итог `ГОТОВО` / `ЗАМЕЧАНИЯ` / `БРАК`, пункт «Показать последний отчёт». Кнопка К-3 с `EnableCheckCommand`, COM-метод для e2e (Т-9).
**Тесты:** unit на правила и разбор отчёта; e2e группа C: чистое изделие → ГОТОВО; изделие без `Операции` → ЗАМЕЧАНИЯ; битая ссылка → БРАК; повторный запуск переносит прежний отчёт.

## Этап 3. К-2 «Экспорт для производства» (Ш6, Т-25…Т-31)

`Sw/ExportService.cs`: PDF на каждый чертёж (цветной, все листы, параметры как у SaveAsPDF, с восстановлением настроек), DXF развёрток со слоем линий сгиба (`ExportToDWG2`, R2000, мм), IGS деталей из профиля с временной СК по оси, отчёт `_Экспорт.txt` с контрольными суммами, поведение «черновик / выдано» по `_Выдано_*.txt`.
**Тесты:** e2e группа X на фикстуре A и боевой прогон; проверка имён файлов по Р0-7 и Т-28.

## Этап 4. К-1 «Сделать независимым с чертежом» (Ш8, Т-19…Т-24)

`Core/OrderNumbering.cs` (следующий свободный номер в децимальной системе), `Sw/IndependentService.cs`: `AssemblyDoc.MakeIndependent`, копия чертежа с перепривязкой видов, очистка ревизии и реквизитов, отчёт; эталон в `02_БАЗА` не изменяется (контрольная сумма).

## Этап 5. К-7 «Новая ревизия» и К-8 «Снимок эталона» (Ш9, Т-48…Т-55)

`Sw/RevisionService.cs` (Revision и заметки `Revision2…5`, строка в `Изменения.xlsx`, файлы `_ИзмN`, прежние — в `_Аннулировано`), `Sw/EtalonService.cs` (копия в `_Версии\<дата>_ИзмN`, применяемость по открытым заказам).

## Этап 6. К-5 «Выдать в производство» и К-6 «Закрыть заказ» (Ш10, Т-13, Т-14в, Т-38…Т-47)

Справочник `Нормативы_производства.xlsx`, `Core/CutPlan.cs` (раскрой хлыстов), окно «Цвета покраски» (RAL и фактура, комплекты), сводная заявка по образцу 85_Т с защитой листов, копирование в `04_ПРОИЗВОДСТВО`, `_Выдано_*.txt`; К-6 — Pack and Go заказа и перенос в архив `Y:` с проверкой копии.

---

## Порядок и сроки

Этапы идут последовательно; каждый заканчивается публикацией на NAS только после зелёных тестов и боевого прогона. Если цеху раньше нужна выгрузка DXF/IGS — этапы 2 и 3 меняются местами (К-2 тогда проверяет изделие «облегчённо», без К-3).
